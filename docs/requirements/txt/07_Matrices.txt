Cooperative Retail and Distribution System
07  -  Supporting Matrices
Baseline requirements package  --  document 7 of 10
Five reference matrices in one document. The RBAC and report catalogues are skeletons  --  both are explicitly flagged in the SRS as needing a finalisation workshop before build; the other three are the settled design.1. RBAC Permission Catalogue (skeleton)
Illustrative role set. The system implements a permission catalogue and role builder (FR-SEC-010); this table is the starting default template set, not a closed list.
RoleRepresentative permissionsCashierSell, park/resume, same-till/same-session refund, apply system-selected discount rules, print receipt. No price or master-data edit, no write-off, no override without supervisor PIN.Shop supervisorCashier permissions plus: cross-till refund (online), till open/close, cash drop, price-override escalation, offline write-off draft capture.MPCS accountantCustomer account management, payment-receipt posting, statement generation, stock-adjustment approval within threshold, write-off approval.MPCS GM / chairmanMPCS accountant permissions plus: price-list and discount-rule authoring, lateral-transfer approval, shop user administration, higher-value write-off and adjustment approval.Distributor procurement / accounts (equivalent roles)Mirrors MPCS roles at distributor scale; additionally manages its Federation-facing order and credit relationship.Federation procurement officerMarket price intelligence entry, supplier and contract management, RFQ/quotation register, landed-cost entry.Federation marketingFederation-recommended price entry (advisory only), federation SKU catalogue authoring.Federation accountsFederation-side payment receipts, statements, VAT summary reports.Federation adminSKU promotion (local-->shared) and duplicate-merge, series registry administration, global reference data.Auditor (entity-scoped)Read-only access to their own entity's documents and audit trail.Auditor (Federation)Read-only access to all entities' documents and audit trail.Co-operative Department (external)Time-bound, read-only, granted and revoked explicitly; every grant is audit-logged.Support bridge / NOC operatorSync-health and queue monitoring, incident flagging. No transactional or master-data access.Helpdesk agentRead-only diagnostic view for a reported issue; cannot reset MPCS-tier passwords (MPCS self-administers).2. Visibility Matrix
Sees -->ShopMPCSDistributorFederationSourceOwn entity's data✓ (role-scoped)✓✓✓entity-scoped / global for fedOwn shops (all of them) -- ✓✓✓n/aSibling shop under same MPCS✗n/an/an/aM-05Counterparty's trading documentsn/a✓ (own trades)✓ (own trades)✓n/aAnother entity's internal data (margins, shrinkage, local suppliers)✗✗✗✓ view-onlyM-02 - M-04, M-09Admin rights over the tier belown/a✗ (MPCS admins own shops)✗ (own shops only)✗ (none downward)M-08Auditors: entity-scoped for MPCS/Distributor auditors; Federation auditors see all entities, view-only. The Co-operative Department holds an external, time-bound, read-only role granted by Federation.
3. Audit Event Catalogue
CategoryEventsAuthenticationLogin, failure, lockout, operator switchPricingPrice override, manual discount (legacy  --  superseded by rule engine), preferential-price application, discount/markdown rule create or changeTill operationsVoid, refund, no-sale drawer openStockStock adjustment, write-off request and approval, repack execution and reversal, GRN discrepancy raised/resolvedFinancialCredit-limit override, payment-receipt posting and reversal, cheque bounce reversalDocumentsDocument void, series registrationAdministrationRole/permission change, user create/deactivate, SKU and price master change, customer account changeSystemSync anomaly (gap detected, duplicate rejected), regulator-access grant/revoke4. Config-at-Setup Register
Not requirements  --  values collected at entity or shop onboarding, per NFR and FR items that specify "configurable."
Config itemNotesCredit headroom warning thresholdPer relationship  --  % of limit at which the banner appearsOrder lock window before ETASeller policy default, overridable per orderInterest default on overdue balancesSet by permitted role per relationship (system supports; not enforced without accounting integration)Discount cap by rolePercentage or value ceiling before supervisor override requiredETA-breach escalation threshold and alert recipientsBy role/permissionLoss/shrinkage tolerancePer SKU or SKU group, before adjustment requires escalationStock-adjustment approval thresholdValue banding by SKU or SKU group marginCash-tender rounding ruleDefault: round to nearest rupee on cash onlyIdle-lock durationTill 2 - 3 min / back office 10 - 15 min / admin 5 min, all configurableSinhala/Tamil input method availabilityProvisioned per shop at onboardingMinimum connectivity specRollout go-live gate per site (~2 Mbps down / 0.5 up, router-based)5. Report Catalogue (placeholder)
ReportStatusTill Z-report / day-sales summaryThermal, offline-capable, phase 1Till session reportThermal, offline-capable, phase 1Statement of account (entity or customer)A4, at configured frequency, phase 1Stock position and ageingA4, phase 1Discrepancy and claim registerA4, phase 1Loss/shrinkage by category and by shopA4, phase 1  --  visibility per the matrix aboveDiscount/markdown cost by ruleA4, phase 1VAT summary (output/input by period)A4, phase 1Supplier performance (fill rate, lead time, rejections)A4, phase 2Regional/seasonal sales analysis (BI)Phase 2Full catalogue  --  remaining reportsTo be finalised in a decision-driven reporting workshop (Document 5, FR-REP-050)