# PROGRESS.md — where the work stands

Updated after every ticket, in the same commit. Three headings: Done, Next, Deviations. A person or tool arriving cold reads `AGENTS.md`, then `docs/README.md`, then this file, and can continue.

Last updated: 26 September 2026, branch `fix/m1-roles-users-grants-review`.

## Done

- **Step 0 (17 Sep).** `AGENTS.md` is the tool-neutral instruction file; `CLAUDE.md` and `GEMINI.md` point to it. `docs/README.md` is the documentation operating guide. `docs/tools/extract-text.sh` regenerates the read-only extracts; all extracts regenerated as UTF-8 with LF endings. ADR and change-request templates added; LF enforced for `.md`, `.txt`, `.sh`. Commit `cf0d836`.
- **Step 1 (17 Sep).** Orientation reading done: `AGENTS.md`, `docs/HANDOVER.md` (since retired), `docs/DECISIONS_PENDING.md`, then 17A and 19A end to end, then 19, 18 and 32. Checkpoint 1 reported to the architect; its open decisions are under Next.
- **Document update (18 Sep).** Register v0.5 (Word, live, `docs/requirements/`), 18 v0.3.1, 14 and 22A added, 00_Start_Here v1.1, doc 10 v0.4 with `docs/DECISIONS_PENDING.md` as its Markdown source; extracts regenerated; CR-00_Start_Here-1 raised. Commits `ee32343` to `500102a`. Both Drive folders match the repository file for file.
- **HANDOVER.md retired (18 Sep).** Everything actionable in the briefing now lives in the register, doc 10, `docs/README.md` and this file; its one unresolved point became CR-28A-1 (cost of goods sold from `stock.moved.v1`, not from the receipt bundle). The original stays in git history, commit `ed20626`.
- **Identity follow-ups of the review (26 Sep, branch `fix/identity-review-2`).** The second review of PR #116. **A location must belong to the entity** (`ScopeFilter.holds`, `LocationOwners`): an entity-wide holder may name a location only when M1's `party.location` gives it that entity as owner (read as the federation viewer, cached an hour), else `scope.invalid`. **Test tokens carry no entity-wide scope** (`TestIdentityProvider`): the scopes come from M1's assignments; the suites whose users have no assignment (hello, M1, M2, M3, sync, collation) opt in explicitly (`entityWideToken`, `entityWideHeaders`), and `TokenAuthenticationPostgresIntegrationTest` proves a shop-only user is refused entity-wide and at another shop. **`security.mfa.freshness` at most twelve hours.** **`AcceptedClientsValidator` decides on `azp` alone when there is one.** **`JwtClaimsMapper` logs an ERROR at start** when password-reauth-counts is on with a non-development issuer. **Documented**: `createUser` makes OWN logins only, the `resetTotp` race, cache invalidation "within a minute", `compose.two.yml` names the password-reauth flag. **Not here**: the `OutboxWriter` listener finding (branch `fix/event-backbone-review`). Deviations: the step-up window and the dev realm's password grant, below.
- **Sprint 0 work done before the handover** by the platform pair (16 Sep, PRs #1 to #6): parts of S0-02 (app, three profiles, health endpoint), S0-03 (kernel `api` interfaces and in-memory stubs), S0-04 (per-module Flyway runner, baseline migration) and S0-05 (architecture tests, pipeline scripts). Each is partial; see Deviations.

- **Pre-module fixes (18 Sep, branch `feat/s0-foundation-fixes`).** Identifiers renamed to coop-erp (ADR-051): YAML prefix `coop-erp:`, `COOP_ERP_ROLE`, `CoopErpApplication`; Java packages and the till namespace stay `lk.coopfed.knoweb`. Kernel API widened to the 17A/19A shapes (ScopeContext with scopes and active scope, PolicyClass with API_CLIENT and DEVICE, IdempotencyStore that stores results, full document base records); baseline roles created only when absent (V0001 checksum changed: reset local databases once); ArchitectureTests gained the layer rule and the own-schema entity rule. Sprint 0 review of 18 Sep: S0-02 and S0-04 done; S0-03, S0-05, S0-06, S0-09, S0-11 partly; S0-01 and S0-07 open against 17A §14; details in the review notes of that day.

- **S0-10 local environment (19 Sep, branch `feat/s0-10-local-environment`).** Built on the compose skeleton of pull request 15. `infra/compose/compose.yml` now runs PostgreSQL 16 (ICU locale provider, volume, init script), PgBouncer in transaction pooling, RabbitMQ, MinIO with the four buckets of 17A §10 (`audit-anchors` with object lock), Keycloak importing `realm-dev.json` (realm `coop`, client `coop-erp-web` with PKCE, users `fed-admin`, `mpcs-admin`, `cashier`, password `dev`, in English, Sinhala and Tamil), Mailpit, the backend and the Vite web client, all with health checks and pinned image versions. `compose.two.yml` and `nginx-two.conf` add a second backend behind nginx. The backend image is built by Jib (`make image`). The Makefile targets `image`, `up`, `up-2`, `down`, `reset`, `migrate`, `seed`, `build`, `test` and `gen-clients` are real; `tools/gen-clients.sh` generates a client for every slice. Three database users replace the superuser login: `postgres` for the init script and seeds only, `coop_migrator` (owns the database, used by Flyway, direct connection) and `coop_app` (member of `app_rw`, used by the application through PgBouncer), so row-level security now applies to the application. Hello sample rows are in `seed/hello/greetings.dev.sql`. Verified on the build machine from an empty state: `make up` and `make up-2` pass; every service healthy; the application user sees no greeting without a scope and is refused an UPDATE; each dev user obtains a token carrying `ent`, `cls`, `lang` and `roles`; six requests through nginx split three and three between the instances; `make down` and `make reset` behave as documented. README carries the fifteen-minute start.

- **S0-12 hello module (19 Sep, branch `feat/s0-12-hello-module`).** The skeleton of pull requests 15 and 16 is now a working template. Migration rewritten to the 17A §6.3 policy template (it read a setting the kernel never sets, so no row could match; the timestamp default shifted instants by the Colombo offset); real entity, repository, queries, handler (guards, mutation, audit, event) and domain event; controller following a slice that now has list, get and register with 201, the idempotency header and problem responses. New 17A kernel stubs: `ScopeSessionAspect` puts the caller's scope on the transaction, `DevScopeArgumentResolver` builds the scope from headers, `IdempotencyFilter`, RFC 9457 problem responses, one CORS filter. Audit coverage is enforced, because the audit and outbox tables arrive only with K-04 and K-05: two new build rules (every command handler calls `audit.record` and `events.publish`; nothing but a command handler writes to the database), each with a fixture proving it bites; the audit and event stubs now refuse what the real tables will refuse (non-catalogue type, missing subject or scope, unversioned event type, call outside a transaction); `KernelRecorder` in the test support package gives every integration test the audit records and events sorted by commit or rollback. Testcontainers integration tests (`make test-int`, 16 tests, run with the JVM in Asia/Colombo, also in CI) prove tenant isolation, the federation view, fail-closed without a scope, UPDATE and DELETE denied, idempotent retry, one audit call and one event, rollback when the event fails, and that the stored instant is exact under a non-UTC server zone (Hibernate is pinned to UTC; the API returns `createdAt` in UTC and the page shows Colombo time). Message ids in three languages on both sides; the web page registers and lists greetings in English, Sinhala and Tamil with the EN fallback tag. Verified through the running stack and PgBouncer. `hello/README.md` is the living guide and holds the proof table. After pulling run `make reset` once: `V0001__greeting.sql` changed.

- **S0-13 scaffolder (19 Sep, branch `feat/s0-13-scaffolder-rework`).** `tools/new_module.py` is replaced by `tools/new-module.mjs` (Node, standard library only; Python was an undeclared dependency and absent from the build machine). `make new-module NAME=m2catalogue SCHEMA=catalogue ENTITY=sku` copies the whole hello template: backend package with a module descriptor and a README written for the new module, migration, slice, seed file, the sixteen integration tests, web module; it registers the module in the three i18n catalogues, `web/src/router.tsx` and the web message catalogue (marker comments), then generates the web client. It validates the module against its schemas, refuses to overwrite, treats a folder holding only `.gitkeep` as empty (the old script refused every planned module but one), refuses an aggregate whose class name would clash with an import of the template (`Entity`, `List`) or with an existing aggregate, and has a dry run. The Makefile now finds the POSIX shell of Git for Windows by itself, so every target works from PowerShell and cmd as well as Git Bash (the first real use, from PowerShell, failed on `sed` and `sh`). Sixteen tool tests run in `make test` and CI against a temporary copy of the template, so they do not depend on which modules exist; one of them fails if the tool's module table drifts from `ArchitectureTests` or `FlywayConfig`. Done criterion proved and made repeatable as `make test-scaffold` (also a CI job): a scaffolded `m3pricing` compiled, kept every architecture rule green, passed its sixteen inherited integration tests against PostgreSQL and built in the web client. The two `m2catalogue` placeholders removed by pull request 17 are restored.

- **S0-13 follow-up (19 Sep, same branch).** Two limits removed. (1) `ENTITY` may be a compound name in snake case (`price_list`, `tax_category`), which most aggregates in the guides are; the tool spells it as each place needs (`PriceList` classes, `priceList` variables, `price_list` table, message ids and event type, `/price-lists` in the URL, "price list" in comments and texts), derives the plural from the last word (`PLURAL` overrides) and refuses a name whose audit code would exceed forty characters. (2) The copy's permissions are now recognisable placeholders (`todo.<schema>.<entity>.register`), and two guards refuse them, so a forgotten replacement is a red build on the developer's own branch and not a surprise when 19A K-03 starts enforcing permissions: architecture rule R7 also rejects the placeholder (with a fixture proving it bites), and the new `tools/check-permissions.mjs` (in `make test` and CI) rejects it in handlers and slices and also fails when a `@CommandHandler` permission is not the `x-permission` of any operation in its module's slice, which AGENTS.md required but nothing checked. `make test-scaffold` now scaffolds `m3pricing` with `price_list`, confirms the fresh copy is refused, replaces the placeholders as a developer would, and then runs everything; proved in a clean clone (sixteen inherited integration tests green). Twenty-two tool tests.

- **S0-11 pipeline (19 Sep, branch `feat/s0-11-pipeline`).** The workflow is rewritten on two rules: a job does real work or does not exist, and jobs call the make targets developers run. Fixed: the stale-client check could never fail (it diffed without regenerating) and is now `make check-generated`, which also covers the Modulith module diagrams, now committed under `docs/modules` as 17A §5 says and emptied before each generation; the "E2E" and "Publish" jobs only printed text and reported green, so "E2E" is replaced by `stack-smoke` (`make up-2`, then the new `tools/smoke.mjs`: health, web, a token with scope claims for each dev user, scope isolation, federation view, registration in UTC, a translated problem, the idempotency key, and nginx alternating between two instances) and "Publish" is removed until a registry exists; web tests were never run and `pnpm lint` called a tool that was not installed, so ESLint 9 is configured (its first run found one `any`) and a Vitest suite checks that every web message id exists in three languages with the same placeholders; the package job now builds the Jib image (`make image`), the web bundle, the shared engine and a CycloneDX bill of materials (139 components), keeping the image only for `main`; the nightly runs `make reset`, `make up`, `make smoke`, `make test`, can be started by hand, prints container logs on failure and always stops the stack; Node 24 everywhere; superseded runs of a pull request are cancelled; till steps no longer skip themselves silently. Checked with actionlint, and every command the jobs call was run on the build machine, including `make smoke` failing when one of two backends was stopped and `make check-generated` failing on a changed slice. `tools/check-generated.ps1` and `tools/gen-clients.ps1` are gone: the make targets work from PowerShell.

- **S0-06 OpenAPI-first toolchain, server side (19 Sep, branch `feat/s0-06-openapi-server-generation`).** The web client half existed; the server half did not, so the hello controller was written by hand against its slice. Now openapi-generator 7.25 (generator `spring`, interfaces only) runs in the Gradle build: one task per slice, found automatically, so a scaffolded module needs no build change. Each slice yields `<Tag>Api` and its request and response classes in `<module>.web.generated`, as build output that is neither committed nor edited, with an `Instant` for date-time, plain nullable fields for the 3.1 `[string, 'null']` types, no Swagger dependency, no generation timestamp and no default method bodies. `HelloController implements HelloApi` and has no mapping annotations; proved that an operation added to the slice fails compilation until the controller has it. `kernel.api.CurrentScope` is new: generated method signatures have no room for a scope parameter, and the design names no way for a controller to read the request's `ScopeContext`; the header-based argument resolver of S0-12 is replaced by its stub `DevCurrentScope`. What every slice shares moved to `openapi/common.yaml` (the Idempotency-Key header, the `Problem` document, the 400 and 422 responses) and is referred to, not copied. Two new safeguards, each proved to bite: architecture rule `controllersImplementTheirGeneratedApi` (fixture with hand-written mappings) and `OpenApiSliceRulesTest` (3.1, `/v1/`, unique `operationId`, exactly one tag, `x-permission` everywhere, Idempotency-Key on mutating operations; no key without a value, which is what a comma inside an inline description becomes; a deliberately broken slice must report nine violations). Verified (again on 20 Sep, after the two decisions below were built): 58 unit and 21 integration tests, 22 tool tests, `make check-generated`, `make test-scaffold` in a clean clone (the copy got its own `PricingApi` and passed its 21 inherited tests), and `make smoke` against the running stack with the new Jib image.
- **S0-05 close-out: boundary rules and pipeline checks (20 Sep, branch `fix/s0-05-boundary-checks`).** Review of the merged work against 17A section 5 found the Java rules sound and the two scripts not: neither had a test, so the done criterion (a deliberate violation fails the build) was unproven for them, and both let real mistakes through. `tools/check-i18n.mjs` only compared the three files with each other; it now also fails for a message id the Java code answers with (first argument of `new ProblemException(` and `messages.t(`) that is in no catalogue, for an empty text, for translations that do not carry the same `{0}` placeholders, and for the ASCII apostrophe, which a message format reads as a quote and drops together with the value after it. Its first run found `document.immutable`, thrown by the document base and in no catalogue; added in three languages. `tools/check-schema-ownership.mjs` looped over its own module list, so a misspelt or unregistered migration folder was skipped in silence; it now walks the folders and fails on an unknown one, strips comments first, fails on a table or index named without its schema, also reads policies, triggers, views, functions, sequences, types, grants and `CREATE SCHEMA`, and checks the seed folders as well as the migrations. It takes its module list from the scaffolder, and a test keeps that list equal to `ArchitectureTests.SCHEMA_OWNERSHIP` and to what `FlywayConfig` migrates. `tools/checks.test.mjs` (21 tests, in `make test`) breaks each rule on purpose. The two unused wrappers `check-i18n.sh` and `check-schema-ownership.sh` are removed. `ArchitectureTests`: the schema rule now also covers an `@Entity` with no `@Table`; three new rules, each with a bite fixture: a published package (`api`, `query`) depends on no JPA type and on nothing from `internal` or `web`; a `DomainEvent` is a record in `api` with a `TYPE` like `module.thing.happened.v1`; a `@CommandHandler` has a `@Transactional` handle method. The extra kernel-internals rule that 17A does not list stays: Modulith already enforces it, the rule says so in one readable sentence. `hello/README.md` gained the table "What the build checks for you". Verified: 65 unit tests (14 architecture rules, 16 bite tests), 43 tool tests, `make test`, `make check-generated`, and `make test-scaffold` in a clean clone: the copied `m3pricing` passed every new rule and its 21 inherited integration tests. No runtime code changed except the three catalogue files, so the stack smoke was not rerun. **Pipeline fix in the same branch:** with `gh` access on 20 Sep the runs were read for the first time: every run since S0-13 (#21) had failed, in one job only, Scaffolder proof. Cause: `backend/gradlew` was committed as mode 100644, the workflow ran `chmod +x` on it, that mode change made the working tree dirty, and `make test-scaffold` refuses a dirty tree. It never showed on Windows. Both wrappers are now committed as 100755 and the five `chmod` steps are gone. The other jobs (Build, verify, test; Stack smoke with two instances; Package) were green on Linux throughout, S0-06 included. Branch protection and rulesets are not available for a private repository on the current GitHub plan (HTTP 403, Upgrade to GitHub Pro), so a red pipeline still cannot block a merge; the same applies to CODEOWNERS.
- **S0-03 close-out: kernel API and stubs (20 Sep, branch `fix/s0-03-kernel-api`).** Review against 17A section 4.3 and 19A. Two defects in the message stub: a language the catalogue does not have (a browser set to German) got the message id as the text, and a catalogue file that could not be parsed was swallowed and every message of that language became its id. `JsonMessages` now reads the three files at start and refuses to start on a missing or broken one; an unknown language, and an id one language lacks, fall back to English. Two interface changes decided by the architect while nothing uses them: `kernel.api.BusinessClock` is gone and `java.time.Clock` (UTC, `KernelClockConfig`, not a stub) is the one clock, as 19A section 13 says; `BusinessDate.current` takes the location id, not the scope, because a business date belongs to a location and an entity-wide scope has none. `CalendarBusinessDate` reads that clock and takes its zone from `coop-erp.business-timezone`, as `SeedConfigRegistry` now does: the zone was written in three places. New architecture rule with a bite fixture: a business module never reads the wall clock (`Instant.now()`, `LocalDate.now()`, `System.currentTimeMillis()`, `new Date()`). New `DevStubsGuard`: a warning at every start that identity and scope come from unauthenticated headers, and a refusal to start under a `prod` or `production` profile. Left for 19A, on purpose: `ConfigRegistry` has no decimal or duration getter (K-11); the hello module shows no use of `Clock`, `BusinessDate`, `ConfigRegistry` or the document base, because a greeting needs none. The first pipeline run of this branch failed, usefully: `JsonMessages` had gained a second, test-only constructor, and with two constructors and no `@Autowired` Spring looks for a no-argument one, so the application did not start and all 21 integration tests failed. The unit tests had called the constructor by hand and could not see it. Fixed, and `KernelWiringTest` now builds the database-free kernel beans through Spring from their real constructors and property names (shown to fail without the fix), so the next mistake of this kind fails in `make test`, without Docker. Verified on the build machine: 81 unit tests (15 architecture rules, 17 bite tests), 43 tool tests, `make test`, `make check-generated`. **Not verified on the build machine:** the integration tests, `make test-scaffold` and the stack smoke, because Docker Desktop would not start there on 20 Sep (next entry); the pipeline runs all three on Linux, and the pull request is not to be merged before they are green.
- **S0-07 web shell, step 1 of 5: login and session (20 Sep, branch `feat/s0-07-web-login`).** Before this the shell had a login configuration and nothing that ever started a login; the scope banner, the API client, the navigation and twelve components are one-line stubs, and only i18n and the hello page were real. The ticket is built in five pull requests (login; API client; scope banner, module definition and navigation; design tokens, fonts and the first three components; Playwright and the e2e stage). This one: `RequireLogin` around the whole application sends an anonymous visitor to the identity server (authorization code with PKCE, S256) and brings them back to the page they asked for; it never retries a failed login by itself. Tokens live in memory only (doc 30 section 3), renewed in the background; a reload is answered by the identity server's own session without a form. `useSession()` is the one way the client learns who is signed in (user, home entity, policy class, language, roles, from the access token's claims; anything missing falls to the safe side: no entity, class NONE, English). The language of the screen now follows the user's `lang` claim, then `?lang=`, then English. `UserMenu` shows the name and signs out. Decided by the architect for the whole ticket: react-router 6 to 7 and react-intl 6 to 12 now (both went in without a code change; `pnpm audit` reports no known vulnerability for the first time), Keycloak stays on 25 until the login exists and is tested, `openapi-fetch` inside the API client (step 2), the headless component library (open item E-03) waits for M1's first forms. Component tests arrive with this step: jsdom and Testing Library. **Temporary until 19A K-02, and said so in the code:** the backend does not validate the token yet and still reads the scope from the development headers, so from step 2 the client sends the bearer token and headers derived from the token's claims. Deferred with reasons: the scope switcher (the dev realm has no `scopes` claim; needs M1), the step-up dialog (needs a backend that issues the challenge). Verified: 15 web tests, lint, build; on the running stack an anonymous visit to `/hello` lands on the Keycloak sign-in page with `code_challenge_method=S256`; `make smoke`. Typing the development password into the form was left to a person, and to the Playwright test of step 5.
- **S0-07 web shell, step 2 of 5: the API client (20 Sep, branch `feat/s0-07-api-client`).** `shell/api/client.ts` is the one way the web client talks to the API; a module never calls `fetch`. `useApiClient<paths>()` is `openapi-fetch` over the types `openapi-typescript` generates, so paths, parameters, bodies and responses are checked by the compiler against the slice: a changed slice is a compile error in the web client as it is in the controller. Every request gets the bearer token and `Accept-Language`; a mutating request without an `Idempotency-Key` is refused before it is sent, because a key made up inside the client would be new on every retry. `useIdempotencyKey()` holds the key of one user action: the same across retries, renewed when the server has answered, not after a network error. An error answer becomes a thrown `ApiProblem` with the stable `code`, the translated `title`, and `fieldErrors` for a form (the `errors` list of CR-17A-1); the hello form now shows them under each field. The shell takes the `Problem` type from `web/src/generated/common.ts`, which `make gen-clients` now writes from `common.yaml`, so the shell depends on no module. `helloApi.ts` lost its private fetch code and its hard-coded development scope: what a user sees now follows who signed in (the Federation administrator sees every entity, the MPCS users their own). **Temporary until 19A K-02, in one marked function:** the scope headers the 17A stub reads are derived from the token's claims. Not built: the step-up retry (needs K-02). Verified: 22 web tests (headers, the refused keyless mutation, problem and field errors, a non-API error, the key's life), lint, build, 46 tool tests, a scaffolded module gets `usePricingApi` with its own paths, `make check-generated`, `make smoke`.
- **S0-07 web shell, step 3 of 5: scope banner, module definitions, navigation (20 Sep, branch `feat/s0-07-scope-and-navigation`).** `shell/scope`: `ScopeProvider` and `useScope()` give the active scope of the signed-in user (entity, location, policy class, whether it can show data at all), worked out from `useSession()` by the plain function `scopesOf`. The token has no `scopes` claim, so a user has exactly one scope and there is no scope switcher; the state is already a list (`available`), and the switcher of doc 30 section 3 comes with the scope assignments of M1 and the claim mapping of K-02. `ScopeBanner` is real and on every page: "who is acting for which entity, all locations, access class", the class always in words because colour is never the only signal; a user with no entity or class NONE gets a warning with `role="alert"` ("you have no scope; you will see no data"), not an empty strip. No entity names before M1, so the banner shows the last eight characters of the entity id (UUIDv7 ids created together differ at the end, not at the start). **Module registration (17A section 7):** `shell/modules/ModuleDefinition.ts` is `{ id, routes, navItems, requiredPermissions }`; a module exports one from `module.tsx` (hello's `routes.tsx` was renamed) and is listed once in `web/src/modules/registry.ts`, which is where the two `new-module:` markers of the router moved to; `router.tsx` names no module any more. `requiredPermissions` means "at least one of": it filters the navigation and guards every route of the module, put on once by the shell (`shell/modules/assemble.tsx`) so a module cannot forget it; a stricter screen wraps itself in `RequirePermission`. A route the user may not open shows a translated "not allowed" screen inside the frame; an unknown address shows "page not found"; `/` shows a one-line start page; a user with no module at all reads that in the navigation. Nav labels are message ids, and a test fails for one that does not exist or leads to a route the module does not have. The hello page hides the form from a user without `hello.greeting.register` and says why (doc 30: disabled actions state why). **Temporary until 19A K-03, in one marked map** (`shell/auth/permissions.ts`): the three development roles get their permissions by hand (fed-admin everything, so a freshly scaffolded module is visible to one user without touching the shell; mpcs-admin read and register; cashier read only). K-03 replaces the map with the resolved permission set; `hasPermission` and `useHasPermission` keep their signatures. It decides what is shown; the server stays the authority, and it does not check permissions yet either (K-03). **Scaffolder:** registers the copy in `registry.ts`; the copy gets its own `ModuleDefinition` (`pricingModule`, id and route `pricing`, label `pricing.nav`, permissions `todo.pricing.price_list.*`), and a web test now refuses a registered `todo.` permission as the backend check does; `make test-scaffold` replaces the placeholders in `web/src` too and runs the web tests. Styling is one small `shell/shell.css` with literal values; design tokens are step 4. Not built: the scope switcher and entity names (M1), location in the scope (no claim), per-navigation-item permissions (not needed until a module has screens with different audiences). Sinhala and Tamil of the 22 new messages are unreviewed, like the others (QA). Verified: 50 web tests (28 new: scope and the no-scope warning, banner in three languages, navigation shown and hidden by role, the not-allowed screen, routes assembled from definitions, duplicate ids refused, the registry rules), lint, build, 47 tool tests (one new), `make test`, `make check-generated`, `make up` and `make smoke` (11 checks); a module scaffolded in a throwaway clone builds and lints, its web tests fail on the placeholders and pass once they are replaced, and `make test-scaffold` passes there end to end; the running client redirects an anonymous visitor to the identity server. Not verified by a tool: the screens after login, because no tool may type a password; the architect tries the three development users in the browser. **Review of the step (same day, before the merge):** the route guard wrapped `route.element` only, so a parent route that only groups its children (/party/entities, /party/locations: the shape of every real module) would have rendered nothing at all; it now renders the matching child, handles `Component` routes, and refuses a `lazy` top-level route with a clear error instead of leaving it open. `tools/check-permissions.mjs` now also reads the web modules: a permission named in `requiredPermissions`, `useHasPermission(...)` or `<RequirePermission anyOf>` must be an x-permission of that module's slice, because a wrong code hides the screen from everybody without any error; shown to catch a one-letter typo.
- **S0-07 web shell, step 4 of 5: design tokens, bundled fonts, the first three components (20 Sep, branch `feat/s0-07-design-foundations`).** `web/src/design/tokens.css` is doc 30 section 2.1 as CSS custom properties and nothing else (`:root` is its only selector, and a test says so): the one type scale 12/14/16/20/28/40 px (40 for money totals only), line height 1.5 everywhere including headings (Sinhala and Tamil stack marks), two weights (400, 600), the 8 px grid (`--space-1` to `--space-8`, 4 px as `--space-half`), targets of 48 px with 12 px between them, neutral surfaces, ONE accent (`#1b5e20`), a text and a background colour for each of draft, issued, disputed, void and alert, and a training colour used for nothing else. Lengths are rem (a user's larger browser font enlarges the screen, not only the letters), hairlines px. **Contrast is a test, not a promise:** `design/contrast.ts` lists the 21 foreground/background pairs a screen may use, and `design/tokens.test.ts` computes the WCAG 2.2 ratio of each from the real file; a colour token that is in no pair and not on the short "decorative" list (`--color-border` only) fails too, so a new colour cannot escape the check. Measured: text 14.76:1 on the page and 13.53:1 on the subtle surface, muted text 6.46 and 5.92, control edges 4.83 and 4.43 (need 3), accent 7.87 and 7.21, white on accent 7.87, chips draft 7.56, issued 6.93, disputed 6.24, void 8.66, alert 6.21 (alert text on the page 7.33), white on training 9.39; shown to bite by lightening the muted grey (2.50:1, three pairs named). No dark theme: doc 30 asks for none and every pair would need its own check. **Fonts:** Noto Sans, Noto Sans Sinhala and Noto Sans Tamil from the npm packages `@fontsource/*` 5.3.0 (OFL 1.1), imported in `design/fonts.css` and served from our own bundle; nothing is fetched from a third party at run time. Weights 400 and 600, one subset per package (latin, sinhala, tamil); Latin letters and all digits come from Noto Sans, the first name in `--font-ui`, which also keeps digits of one shape in three languages. `pnpm build`: `dist/` 431,531 bytes before, 784,834 after; of the difference the six woff2 files are 131,128 bytes (Latin 26.6 kB, Tamil 30.1 kB, Sinhala 74.4 kB for both weights) and their six woff fallbacks 193,828 bytes that no current browser requests (the packages' CSS names both; dropping woff means writing the `@font-face` rules ourselves: not done, it costs disk on the server and nothing on the wire). A browser downloads a file only for a script the page shows: an English screen takes 26.6 kB. JavaScript 430.41 to 451.91 kB (gzip 131.83 to 137.99), CSS 0.77 to 7.61 kB. `shell.css` and the hello page are on tokens; a module page takes its shape from the shell class `shell-page`. **Enforced:** `design/moduleStyle.test.ts` reads every source file under `web/src/modules` and fails on a hex colour, `rgb(`/`hsl(`, or a px, rem, em or pt literal, naming file and line; a plain line-by-line text search (comment lines and test files skipped) with its own bite tests, shown to stop a `"#b00020"` put back into `HelloPage.tsx`. `shell.css` may not contain a colour literal either, nor a `var(--...)` that `tokens.css` does not define (a misspelt token is silent in a browser). **MoneyDisplay** (`Rs 1,234.00`, tabular figures, size `total` = 40 px): the digits are made by `formatMoneyDigits` in `shell/i18n/formats.ts`, beside the date formatting, as text from start to finish: split at the point, commas into the whole part, fraction padded to two. **The client never rounds and never cuts:** `1234567.891` is shown as `1,234,567.891` and a unit cost `12.3450` as it arrived, because the server is the authority for rounding, a rounded value is one the server never computed, and a cut one hides that something upstream sent the wrong scale. No `Intl.NumberFormat`, so no browser can choose Sinhala or Tamil digits or lakh grouping. Accepts `string | number` (a number through `String()`, exponent forms refused); `-0.00` shows as `0.00`; anything that is not a plain decimal (`abc`, `NaN`, `null`, `1,234.00`, `1e3`, non-Western digits) gives a dash with the spoken text "Amount not available", never `NaN`, never an exception. A negative amount shows U+2212 and is read out as "minus Rs 250.00", because screen readers disagree about the sign and some skip it. The currency mark is a message (`Rs`, `රු.`, `ரூ.`: for QA to confirm). **StateChip:** five looks (draft, issued, disputed, void, alert), each colour pair plus its own symbol plus the word; a module's state machine passes its own translated `label` and picks the look; adding a look is one line in each of four places and the tests name the one that is missing. The symbols are five inline SVG drawings: the "one icon set" of doc 30 belongs with the component library (open item E-03, deferred), said in the code. No strike-through on void: a line through Sinhala or Tamil letters is unreadable. **TrainingBadge:** nothing outside training mode; in it a band across every page that cannot be closed and says in a sentence that nothing here is real, plus a frame in the training colour around the whole page and a changed header (three signals). A labelled region, no focusable element. The backend has no training mode: `RootLayout` passes `isTrainingMode()`, which reads the build flag `VITE_TRAINING_MODE=true` (put it in `web/.env.local`; not in compose, where it would win over the file); the real source is a later kernel/M6 concern (T-04) and changes that one function only. **`/_design`:** a shell route, not a module, in no navigation, behind the login like everything and behind no permission (it shows no data and calls no API): type scale, spacing, every colour pair with its measured ratio and its minimum, MoneyDisplay against good and bad input, the chips, the badge, all in the user's language. It is in the production bundle too (QA reviews translations there); it adds no route shape that passes the guard of `assemble.tsx`. Also: the unused message id `hello_world` removed (nothing referred to it, tools included); `LangFallbackTag` lost its inline literals; `vite.config.ts` sets `test.css: true`, because Vitest otherwise hands a test an empty text for a `.css` file and the contrast test would check nothing; the hello README and the README template of `make new-module` gained the rule "tokens only; money through MoneyDisplay". Deferred with reasons: the other nine components stay stubs until the module that needs them (DocumentHeader/Lines, Timeline, ApprovalBar, ReasonCapture with M4/M5; ItemCard with M2; FreshnessStamp with M8; ExceptionQueue with M1 governance; AttachmentCapture with the first GRN discrepancy), built on the base library of E-03; the totals block of the till (subtotal, reductions, tendered, change) is M6 and is composed of MoneyDisplay; toasts; quantities with unit suffix. Sinhala and Tamil of the 28 new messages are unreviewed, like the others (QA). Verified: 101 web tests (51 new), lint, build, 47 tool tests, `make test`, `make check-generated`, `make test-scaffold` end to end in a throwaway clone (its first run died in Vitest with "cannot allocate memory" because `make test` was running beside it on a machine with 4 GB of free commit; alone it passes), `make up`, web container restarted for the new packages, `make smoke` (11 checks); the dev server serves the six woff2 files from `localhost:5173`; an anonymous visit to `/_design` lands on the Keycloak sign-in page with `code_challenge_method=S256`. Not verified by a tool: how the page looks after login, because no tool may type a password; the architect looks at `/_design` as each development user and once with `VITE_TRAINING_MODE=true`. **Review of the step (same day, before the merge):** the money formatter, the contrast test and the literal check were read and run again (101 web tests). One gap closed: `make test` did not run the web client at all, so a developer who ran it before a push missed the web tests and both new design checks; it now runs `pnpm lint` and `pnpm test` too, the pipeline's separate web lint and test steps are gone (one command, the same on a laptop and in the pipeline), and the nightly job sets up pnpm, which it now needs. For whoever runs heavy jobs on the architect's build machine: one at a time; C: has 2 to 3 GB free and the page file cannot grow, and two test runs side by side died with "cannot allocate memory". **Found by the architect in the browser, fixed before the merge:** after a fresh login the address was /_design and the page shown was the start page. A defect of step 1, not of this step: the router was a constant created when the file loaded, that is while the browser was still on "/?code=..."; the login callback then restored the address with `history.replaceState`, which no router hears. The router is now made by `createAppRouter()`, called by App on the first render after login; two tests guard it. It had not shown before because a user who was already signed in never takes that path, and because no tool may type a password, which is the case for the Playwright test of step 5. The void chip gained a diagonal hatch and a double edge (a cancelled stamp; nothing touches the letters), so it differs by symbol, word, hatch and edge before colour; the darker stripe is a token and a pair in the contrast test (7.33:1). 103 web tests.
- **S0-07 web shell, step 5 of 5: the Playwright smoke test and the e2e stage (20 Sep, branch `feat/s0-07-playwright-e2e`).** Until this step nothing automated had ever signed in: every web test runs in jsdom with a session made up in the test, which is why the one defect that reached the architect's browser (a deep link lost at login, found and fixed in step 4) was invisible to the whole suite. `web/e2e` is eleven Playwright tests in a real Chromium against the running stack, typing the development password on Keycloak's own page — the one thing test code may do and a person or an agent may not. `make e2e` runs them the way `make smoke` runs the API checks: the stack must already be up (`make up` or `make up-2`; `TWO=1` is accepted and changes nothing, because nginx fronts both backends on the same port 8080 and neither the browser nor the tests can tell the difference, which is the point). **What they prove.** For each of the three development users: an anonymous visit to `/hello` lands on the identity server, and after signing in the browser is back on `/hello` — not on the start page — with the scope banner naming the user and the access class, the navigation entry and both headings in that user's language; Sinhala for mpcs-admin and Tamil for cashier although the browser asks for English, because the language comes from the token's `lang` claim. **The regression that is the reason for this step:** sign out, ask for `/_design`, sign in, and the design page must be shown. Shown to bite by creating the router at module load again, exactly as before the step-4 fix; it then fails with `expect(locator).toBeVisible() failed … getByRole('heading', { name: 'Design reference', level: 1 }) … element(s) not found`, and the reload test with the same message for the hello heading. Then: a reload keeps the user signed in without a form (tokens live in memory, the identity server's own session brings them back); the cashier sees the greetings and the read-only note and no form, mpcs-admin the form, the Federation view the greetings of both entities and the society only its own; a registered greeting appears in the list and the same text again shows the server's Sinhala refusal (`hello.greeting.duplicate`); signing out really ends the session and the back office is not reachable afterwards; an unknown address shows the translated "page not found" inside the frame. **Decisions where the brief left room:** the assertions read the real catalogues — `web/src/shell/i18n/messages.ts` for the client and `backend/app/src/main/resources/i18n/si.json` for the server's message — instead of Sinhala and Tamil typed into a spec, so a changed translation changes the test with it and a deleted message id fails loudly; every locator is a role or a text, never a class name; each run registers a text nobody has registered before, because the greeting table is insert-only and the stack is not reset between runs, so `make e2e` is repeatable against a stack it has already run against (it was run twice in a row to prove it); no retries, because a retry hides exactly the kind of fault this step exists to catch and every test starts in a fresh browser context; Chromium only (one browser keeps the pipeline short, and the back office is used on managed machines, not on whatever a visitor brings); two workers (more browsers do not make the stack answer faster and this build machine has little free memory); and the browser locale is fixed to English deliberately, so that Keycloak's own page always speaks one language while the back office speaks three. **Three changes the tests forced, each small and said so in the code:** `messages.ts` imports its JSON catalogue with the standard attribute `with { type: "json" }` (and so does the line `make new-module` writes into it, with the scaffolder test), because Node refuses a JSON import without it and the tests read that file directly rather than copying it; `web/tsconfig.e2e.json` is a second TypeScript project, so Node's globals are on for the tests and stay off for the browser code, where `process.env` would compile and then be undefined; `vite.config.ts` excludes `e2e/**` from Vitest and `playwright.config.ts` takes `*.spec.ts` only, so neither runner picks up the other's tests. ESLint covers `web/e2e` rather than ignoring it: it is the code the team will copy for a module's own end-to-end tests, and a typo there otherwise costs a pipeline run to find. **Pipeline:** the job `stack-smoke` already runs the two-instance stack, which is the "two instances: Playwright run alternates instances; nothing breaks" row of the 17A section 12 proof table, so the run goes there: Chromium installed with `--with-deps`, `make e2e` after `make smoke TWO=1`, and `web/playwright-report` with the traces uploaded when it fails. "Playwright run" is gone from the "not here yet" list at the top of `ci.yml`; the Android emulator smoke stays there, it needs S0-08. `make lint-ci` was run before the push, as the rule of the earlier silent-pipeline incident says. **Verified:** `make up`, `make smoke` (11 checks), `make e2e` (11 tests, 23.4 s) and `make e2e` again on the same stack (20.3 s); then `make down`, `make up-2`, `make smoke TWO=1` (11 checks including the alternation), `make e2e` (23.6 s); the regression run above against a deliberately broken router, and the file restored with `git checkout --` and green again (2 tests, 6.1 s); in `web/`: `pnpm lint`, `pnpm test` (103 web tests, unchanged: Vitest does not see the specs), `pnpm build`; 47 tool tests, `make test`, `make check-generated`, `make lint-ci`. Not verified on this machine: nothing in the tests; but Playwright's own browser download could not be used — Node's request to `cdn.playwright.dev` times out here while `curl` fetches the same file in seconds, so the four archives were downloaded with `curl` into `PLAYWRIGHT_BROWSERS_PATH=D:/tmp/ms-playwright` and unpacked by hand. A machine quirk, not a repository one; on the pipeline and on a normal machine `pnpm exec playwright install chromium` does it. **S0-07 meets its done criterion, "Login, banner, hello page in three languages": it is now a command anybody can run (`make up`, then `make e2e`), not a screenshot somebody once took.** Deferred, each with the ticket that lifts it: the scope switcher and entity names (M1 for the scope assignments, 19A K-02 for the `scopes` claim mapping); the step-up dialog (K-02, which issues the challenge); the temporary role-to-permission map in `shell/auth/permissions.ts` (K-03, the permission resolver — until then a hidden button is a courtesy and a direct request still succeeds); the nine other components of doc 30 (each with the module that first needs it); the headless component base library they will be built on (open item E-03). Sinhala and Tamil of every text remain unreviewed (QA).
- **S0-02 and S0-04 close-out (20 Sep, branch `fix/s0-02-s0-04-profiles-and-schema-rules`).** The two merged Sprint 0 tickets nobody had reviewed. **S0-04:** the baseline matches 17A section 6.1 line for line (one safer difference: `scope_class()` also reads an empty setting as NONE), and the hello policies match the template of 6.3 without `ext_view`, which needs `granted_entities()` of 19A. But the schema test that 17A section 6.2 and AGENTS.md both promise ("a table without a policy fails the schema test") did not exist, and nothing checked "no DELETE anywhere for app_rw" or the schema list of the done criterion. `SchemaRulesIntegrationTest` now reads the catalogue of the migrated database: the twelve schemas; every table has row-level security enabled and forced and at least one policy; `app_rw` may not DELETE, TRUNCATE or CREATE. It needs no registration, so a scaffolded module's tables are covered, and a test creates four deliberately wrong tables and expects each reported by name. A table every tenant may read says so with an explicit `USING (true)` policy. **S0-02:** the three roles did nothing. `COOP_ERP_ROLE` set the property `coop-erp.role`, which nothing reads; no Spring profile was ever active, so `application-web.yml`, `-worker.yml` and `-ingest.yml` never loaded (and were identical), and a worker instance would have served the whole API. Now `spring.profiles.active` is `${COOP_ERP_ROLE:web}` (a comma list works: the local stack runs `web,worker`), and `kernel.internal.RoleHttpFilter` (`@Profile("!web")`) answers 404 for everything but `/actuator`, so no module marks its controllers. 17A asks for health and metrics: `metrics` and `prometheus` are exposed beside `health` and `info`, with the liveness and readiness probes, and `server.shutdown: graceful` for the rolling deployment of doc 14. `RuntimeRolesIntegrationTest` starts the application under web, worker, ingest and web with worker; the smoke test checks the readiness probe and `/actuator/prometheus` on the running stack (Spring Boot switches metric exporters off inside tests). Left open, recorded: the actuator endpoints are unprotected until 19A K-02 and doc 35 decide the management port; migrations run from every instance at start (Flyway locks, so two instances are safe), and whether they become a deployment step is for doc 35; one `app_rw` role serves every schema, so native SQL across schemas is caught by review only. Verified: 81 unit and 28 integration tests, `make test`, `make check-generated`, `make up` and `make smoke` (11 checks).
- **Repository automation (20 Sep, branch `chore/repo-automation`).** Asked by the architect: is everything automated that should be. The build and test side was; the hygiene around it was not. Added: `.github/dependabot.yml` (Gradle for backend and till, npm, GitHub Actions, the compose images; weekly and grouped; Spring Boot, Modulith, React, Vite and PostgreSQL held on the lines 17A fixes); Dependabot alerts and security updates switched on; repository settings changed with the architect's consent: squash merge only, the pull request title is the commit on `main`, branches are deleted on merge (24 stale branches had piled up, and #21 had reached `main` with a title that is no conventional commit); `pr-title.yml` checks the title; a pull request template; Spotless with palantir-java-format, ratcheted from `origin/main` so only touched files are checked and nothing is reformatted in bulk (`make format`; `make test` runs the check); `.editorconfig`; a JaCoCo report over unit and integration tests (`make coverage`; a report in the run summary, no floor); job `secret-scan` (gitleaks over the whole history: 56 commits, nothing found); job `notify`: when `main` or the nightly run fails, an issue labelled `ci-failure` mentions the four maintainers and closes on the next green run, because GitHub by itself mails only the person who caused a run, and a failed nightly run only whoever last edited the schedule; the same job mails the team directly (red, and green again) once the five `NOTIFY_*` repository secrets are set, so that mail does not depend on each person's notification settings (the secrets are the architect's to add; not set on 20 Sep); `security-scan.yml` (Trivy over the bill of materials, the web lock file and the image, weekly, never fails the run, keeps one issue labelled `security`); `make hooks` installs an optional pre-push hook; actions moved from v4 (deprecated Node 20) to the current major versions; `make lint-ci` runs actionlint, added after the first push of this branch carried a YAML error in `ci.yml` and the pipeline simply did not start (a broken workflow file shows no failed check, it shows no checks). The first scan found 7 CRITICAL and 31 HIGH findings with a fix available, nearly all through Spring Boot 3.3.5 (Tomcat 10.1.31, Spring Framework 6.1.14): raised as `CR-17A-2`, because the fixes need the 3.5 line and 17A fixes 3.3.x. **Not possible on the current GitHub plan for a private repository:** branch protection, required checks, rulesets, CODEOWNERS, GitHub secret scanning. So the pipeline informs and cannot block; the rule is "do not merge on red". **Decided by the architect, 20 Sep: enforce in one to two weeks**, when the team has settled: `main` accepts pull requests only, no direct pushes, the jobs Build verify test, Scaffolder proof, Stack smoke, Secret scan and Conventional commit title are required, one approving review, administrators included. That needs the organisation on the GitHub Team plan (it is on Free; 10 members) or a public repository; the settings are one `gh api` call once the plan allows them.
- **Dependabot triage and loose ends (20 Sep, branch `chore/loose-ends`).** The first Dependabot run left nine pull requests; a read-only triage read every diff, check and changelog. Merged on its evidence: the backend patch group (#43: Spring Boot 3.3.13, Modulith 1.2.13, ArchUnit 1.5, Spotless 8.10; inside 17A's lines) and nginx 1.29 (#31). Spring Boot 3.3.13 cures `json-smart` only; Tomcat (10.1.42, needs 10.1.58), the PostgreSQL driver, Jackson, Micrometer and Spring Framework 6.1 stay below the fixed versions, so the issue labelled `security` stays open until `CR-17A-2` is decided. Ready when the architect wants them: the React hooks lint plugin 7 (#33), plugin-react 5 (#37), a react-query patch (#32), in that order because they share the lock file; Keycloak 26.7 (#30) now that the Playwright login test exists to prove it: the release notes name no change our compose file or realm needs. **For the till's owner:** the Gradle wrapper 9.7.1 (#41) builds green with AGP 8.7.3 and would close the "8.9 is below 17A's 8.10" point; compose-bom 2026.09 (#40) needs AGP 9.1 and compileSdk 37; the till group (#39) fails on KSP against AGP 8.7.3 and carried Kotlin 2.0 to 2.4, which our rule forbids. That was a gap in `dependabot.yml`: the Kotlin hold existed for the backend only. It now also holds Kotlin, KSP, the Android Gradle plugin and compose-bom majors for `/till`. Two small repairs found on the way: `make lint-ci` failed from Git Bash on Windows because MSYS rewrote the container path (the recipe sets `MSYS_NO_PATHCONV`), and `.gitattributes` pins `.adoc` to LF so generated module diagrams do not look changed on Windows.
- **S0-01 close-out: repository, version catalogue, README (20 Sep, branch `chore/s0-01-repository-closeout`).** The last ticket of the close-out sweep. `backend/gradle/libs.versions.toml` is the version catalogue 17A section 3 asks for: the seven plugins, Spring Modulith, ArchUnit, the Testcontainers override, the formatter and the Java level, each once, with the versions 17A fixes marked; the three build files name no version any more, and the libraries Spring Boot manages stay without one on purpose. Dependabot reads the catalogue. `ADR-052` records the decision of 20 Sep: Gradle 9.7.1, Node 24 and pnpm 12.4.2 stay where 17A names 8.10+, 20 and 9, read as minimums. The README opens with where to read what (`AGENTS.md`, this file, `docs/README.md`, the hello README, the plan) and the branch and pull request rule, and lists `make build`. Two obsolete `.gitkeep` files removed (a migration folder and `infra/`, both long since filled); the ones that keep the reserved module packages and `infra/deploy` stay. Done elsewhere the same day: `.editorconfig`, the pull request template and the unused shell wrappers. Not done, because the GitHub plan does not allow it: CODEOWNERS. For the till's owner: the till wrapper is Gradle 8.9 (17A: 8.10+) and `till/agent-test` of 17A section 2 does not exist; raise or record both in S0-08. Verified: `make test` (format check, 81 unit tests, 46 tool tests), `make check-generated`, and the resolved dependency versions unchanged.
- **Spring Boot 3.5 (CR-17A-2) (21 Sep, branch `chore/spring-boot-3.5`).** The change request was accepted by the architect on 21 September and is applied here: Spring Boot 3.3.13 to 3.5.16, Spring Modulith 1.2.13 to 1.4.13 (the line that matches Boot 3.5), `io.spring.dependency-management` stays 1.1.7 (already current). Everything else moved with the Boot version, because the catalogue names none of it: Spring Framework 6.1.21 to 6.2.19, Tomcat 10.1.42 to 10.1.55, Hibernate 6.5.3 to 6.6.53, Jackson 2.17.3 to 2.21.4, the PostgreSQL driver 42.7.7 to 42.7.11, Flyway 10.10.0 to 11.7.2, Micrometer 1.13.15 to 1.15.12, JUnit 5.10.5 to 5.12.2, AssertJ 3.25.3 to 3.27.7. **The Testcontainers override is gone.** The catalogue pinned 1.21.4 because Boot 3.3 managed 1.19, which Docker Engine 29 refuses; Boot 3.5.16 manages exactly 1.21.4 itself, so the `testcontainers` entry and the `extra["testcontainers.version"]` line in `app/build.gradle.kts` were removed with their comments and the 28 integration tests still run against PostgreSQL 16 in Docker 29.8. **What broke: nothing that failed a test.** Two APIs became deprecated-for-removal and were replaced: `MethodValidationResult.getAllValidationResults()` is now `getParameterValidationResults()` (`RequestValidationHandler`), and `Documenter(ApplicationModules, String)` is now `Documenter(MODULES, Documenter.Options.defaults().withOutputFolder(...))` (`ArchitectureTests`). The `HandlerMethodValidationException` fallback that 17A's S0-06 work wrote against Spring 6.1 — constraint name from `getCodes()`, attributes from `getArguments()` in alphabetical order — was left as it is: Spring 6.2 changed method validation internally but not that shape, and `RequestValidationHandlerTest` proves it by all three routes, so there was no reason to rewrite working code. No configuration property we use was renamed or removed by Boot 3.4 or 3.5 (checked against the configuration metadata of 3.5.16: `management.endpoint.health.probes.enabled`, the actuator exposure list, `server.shutdown`, `spring.lifecycle.timeout-per-shutdown-phase`, `spring.jpa.properties.*` are all current), and `FlywayConfig` needed no change across Flyway 10 to 11 because `flyway-database-postgresql` was already a dependency. **Generated module docs changed and are committed:** Modulith 1.4 writes `format=svg` into every `plantuml::` line of `docs/modules/all-docs.adoc` and orders the modules differently (Config first), and `module-kernel.adoc` gained an "Events listened to" row for `ApplicationReadyEvent`. **The vulnerability scan, the point of the exercise** (same procedure as `security-scan.yml`: `:app:cyclonedxBom`, Trivy 0.67.2 `--ignore-unfixed --severity CRITICAL,HIGH`, `tools/vulnerability-report.mjs`): **6 CRITICAL and 25 HIGH before, 3 CRITICAL and 1 HIGH after**, on the backend bill of materials. (The 7 and 31 the change request quotes were measured on Spring Boot 3.3.5 on 20 September; the branch starts from 3.3.13, which #43 had already merged, so 6 and 25 is the honest baseline.) Gone: Spring Framework, `spring-boot`, `spring-boot-starter-actuator`, `spring-data-commons`, Jackson, Micrometer and AssertJ. **What remains, for the architect:** `tomcat-embed-core` 10.1.55 needs 10.1.58 (3 CRITICAL: CVE-2026-65182, CVE-2026-65905, CVE-2026-68525) and `org.postgresql:postgresql` 42.7.11 needs 42.7.12 (1 HIGH: CVE-2026-54291). Both are versions Spring Boot manages and both would be cured by one line each in `app/build.gradle.kts` (`extra["tomcat.version"] = "10.1.58"`, `extra["postgresql.version"] = "42.7.12"`); no override was added, because overriding a managed version is the thing the catalogue's own comment warns about and the next Boot patch release should carry both. `.github/dependabot.yml` keeps Spring Boot and Modulith on their minor line as before — the hold is by update type, so it works unchanged on 3.5 and 1.4; only the comments that named 3.3.x and 1.2.x were rewritten. ADR-052 gained a dated note that the untested "Gradle 9 with the Boot 3.3 plugin" combination it describes has ended. 17A section 3 still says 3.3.x / 1.2.x and is not re-issued yet; `AGENTS.md` and `README.md` say 3.5 / 1.4 with that caveat.
- **The two security patches ahead of Spring Boot (21 Sep, branch `chore/security-patch-overrides`).** Decided by the architect on 21 September: take both. The catalogue gained `tomcat = "10.1.60"` and `postgresql = "42.7.12"`, and `app/build.gradle.kts` sets Spring Boot's `tomcat.version` and `postgresql.version` from them. **Tomcat 10.1.58, the version the advisories name, was never published** (Maven Central goes 10.1.57, 10.1.59, 10.1.60), so the override takes 10.1.60, the current patch release. An override that Spring Boot has caught up with holds a library back, so the new task `checkVersionOverrides` (part of `check`, so part of `make test`) reads the versions out of Spring Boot's own bill of materials and fails the build when an override is no longer ahead of it; proven by setting the override to 10.1.55, which fails with the instruction to delete it. The Dependabot pull request that brings the Boot patch carrying these versions will therefore go red until the two entries are deleted: that is the intent. Verified: `gradlew check` (81 unit tests, architecture rules, Spotless, the guard), `make test-int` (28), `dependencyInsight` shows 10.1.60 and 42.7.12 on the runtime classpath. **The scan of the backend is now 0 CRITICAL and 0 HIGH** (from 3 and 1; from 6 and 25 before CR-17A-2). Stack smoke, the two-instance run and the Playwright tests were left to this pull request's pipeline.

- **Preparation for the 19A phase (21 Sep, branch `chore/19a-kernel-prep`).** Nothing here implements a kernel service; it is what has to exist before four lanes start K-01 to K-13 at once without colliding. Four things. **(1) The version catalogue** names the libraries the kernel tickets need, so no two lanes edit `libs.versions.toml` on the same day: the OAuth2 resource server starter (K-02), Caffeine (K-03, K-11), the AMQP starter (K-05), ICU4J 78.3 (K-06), the AWS SDK v2 `s3` client and its bill of materials 2.55.1 (K-09) and ShedLock 6.10.0 with its JDBC-template provider (K-12). Every version was checked against Maven Central. The four Spring Boot manages carry no version, as the catalogue header requires; ShedLock is the latest of the 6 line and not of the 7 line, because 7 is built against Spring Framework 7 (Spring Boot 4) and this repository is on Spring Framework 6.2, and `.github/dependabot.yml` now holds ShedLock majors for that reason, the only change the automation needed. The S3 client is the AWS SDK and not MinIO's own client: 19A §9 asks for a provider-neutral client with path-style addressing, which is what MinIO needs and what the same code gives anywhere else, and AGENTS.md forbids coupling to a vendor for exactly this. **Nothing was added to the application's dependencies.** The two starters must not be: the resource server brings Spring Security, whose default chain answers 401 to every endpoint, and AMQP brings a connection factory and a health indicator that turns `/actuator/health` DOWN where no broker runs. The other four are inert, and were still left out, because adding a dependency that nothing uses changes the bill of materials and the vulnerability scan and proves nothing. **(2) Three declarations in `kernel.api`**: the annotation `@EventConsumer` (K-05), `PermissionResolver.allows(...)` (K-03b) and `Sod.assertDistinct(...)` (K-03b), each with no implementation and no bean, so the context starts as before. Only what 19A writes out was declared; everything it names without a signature was left alone and listed for the architect, which is most of the service map: `IdentityProviderClient` and `DeviceCredentials`, `TextNormaliser` and `Formats`, `DocumentService`, `NumberingService`, `DocumentLinks`, `Attachments`, `SyncApi`, `NotificationDispatcher` and `NotificationChannel`, `SnapshotContributor` and `SnapshotChangeLog`, `@ScheduledJob`, and the `Resolved` and `Limits` types of `PermissionResolver`. Three differences between 19A and the code were found and none was fixed here: 19A §1's `ScopeContext` has no `activeScope` component and a `String lang` where the code has a `Locale`; 19A §6 has `Messages.t` return a `Text(value, fallback)` where the code returns a `String`; 19A's service map puts `BrokerAdapter` in `kernel.internal`, which is right, while AGENTS.md names it beside `IdentityProviderClient` as though both were published. **(3) Migration number ranges** per lane, in a new `db/migration/kernel/README.md`: V0010–V0029 security, V0030–V0049 ledgers and events, V0050–V0079 configuration, documents and jobs, V0080 and up language and sync. Only `V0001__baseline.sql` exists, so nothing collides. The README is safe in that folder: Flyway ignores files that do not match the migration naming, and `tools/check-schema-ownership.mjs` reads `.sql` files only; both were checked. **Measured, not assumed:** `FlywayConfig` runs with `outOfOrder = false` and `validateOnMigrate = true`, and a probe against PostgreSQL 16 showed that a V0010 merged after a V0030 has been applied does not run and is not skipped quietly — `migrate()` throws `FlywayValidateException: Detected resolved migration not applied to database: 0010`, and because `FlywayConfig.migrate()` is a `@PostConstruct` the application does not start. A fresh database is never affected, so the pipeline would stay green while developer machines stop working. The setting was not changed; it is in the pull request for the architect. **(4) `KernelRecorder` no longer names a stub.** It was tied to `LoggingAuditFacade` and `LoggingEventPublisher`, so K-04 and K-05 would have broken all twenty-eight integration tests at once. It is now a plain recorder, and the new `KernelRecording` (a `BeanPostProcessor` in the test support package) wraps whichever bean implements `AuditFacade` or `EventPublisher` and reports to it. The real service still runs first, with every refusal it makes; `@Primary` is gone, so a real implementation may carry it. Every assertion means what it meant. Also recorded in this branch: the architect's decisions of 21 September in `docs/PLAN_TO_M2.md` Phase 2, and the aligned range sentence in Part B.4.

- **Clean-up after the pull requests of 22 and 23 September (24 Sep, branch `chore/cleanup-after-23-sep`).** The M1 tickets M1-02 and M1-03 landed (#68, #72, #73, #82, with the review fixes #75 and #76), #74 put three kernel services on `main` wrongly and #78 rolled them back by hand as the architect asked, and #81 committed a scaffolded `m3pricing` module, which the architect on 24 September accepted as M3's start: modules run in parallel with the kernel from here (`AGENTS.md`, plan Phase 3). Three things were put right in this branch: the rollback migration is `kernel/V0006`, not `V0050` (a rollback belongs below the reserved lanes and must not raise the kernel's high-water mark); the `DELETE` exemption for `kernel.idempotency_key` in `SchemaRulesIntegrationTest` is gone with the table; the `components.puml` normaliser that #78 added stays, with its reason in the Makefile. Two things were checked and are not defects: M1's event names (`entity.registered.v1`) follow doc 19, doc 18, doc 32 and every nA guide, which name a unique aggregate and no module; only the hello template (17A) adds a module segment, and `ArchitectureTests` accepts both. And #78 deleted the three #74 migrations from the tree instead of leaving them, against the rule; accepted this once because nothing is deployed and every developer database is recreated (`make reset`, asked of the team on 23 September). Open from the reviews, for the officer command (#82): the 21A guard "caller is the Federation or an entity admin" is missing, the guard tests assert no message id, and the integration test mocks the scope and the ownership query; and the permission it reuses (`gov.user.manage`) is a question for the catalogue freeze, added to `CR-21A-1`.

- **Review fixes for K-03a and K-05 (24 Sep, branch `fix/k05-relay-user-and-consumer-paths`).** #86 and #87 put the command interceptor and the event backbone on `main` in one day; the reviews found the failure paths, and the ones that matter are fixed here. **Database users are applied on every `make up`, not only at creation:** #87 added the login user `coop_relay` to the role script, which ran only when the volume was created, so every existing database lacked it and the worker could not start. The script is idempotent now (`make roles`, run by `make up` before the backend starts), so a new user or membership reaches an existing database without `make reset`; proven on a database that had no `coop_relay`. A migration cannot do this on PostgreSQL 16: the migrator may create a role but not add it to a group it did not create (measured), and a password never enters git; `AGENTS.md` says so. **Consumers no longer redeliver for ever:** a body that cannot be read or a consumer/type pair nobody registered is refused without requeue and logged, instead of coming straight back; the replayer sends a consumer only the types it subscribes to. **The event payload check is a word list** (name, phone, mobile, email, address, nic, passport, password, pin, otp, token, secret, dob and so on), matched as whole words of the field name, with a unit test of what it refuses and what it lets through; the old heuristic let `customerName` and `mobile` in and refused `technicianId`. **A mutating request without a user is refused** (`scope.required`): until K-02 reads the token, the dev stub gave a request with no `X-Dev-User` a fresh random user, so a retry never matched and the command ran twice. **The non-HTTP bypass is logged:** a command from a sync batch, a job or a consumer has no idempotency check yet (K-08 and K-12 give those callers their keys); the interceptor says so at WARN instead of silently. The RabbitMQ health indicator is on in the worker role only, so a web-only instance is not DOWN for a broker it never uses. Left for the author of the two tickets, from the reviews: retries in place so a failed event keeps its order, backoff and a metric when the broker is down, the relay `UPDATE` by primary key (carry `received_at`), `@Profile("worker")` instead of `@ConditionalOnBean` on the relay components, a two-instance test with a real broker (19A names it), a concurrency and a rollback test for the idempotency claim, the three idempotency config keys declared in `application.yml`, a DEFAULT partition for `idempotency_key`, inbox RLS scoped, and the literal relay password default removed.

- **M1-11 BulkRegister (24 Sep, branch `feat/m1-11-bulk-register`).** `POST /v1/party/bulk-register` (21A section 5, multipart; section 8, the "Bulk upload" button of the society register) takes a CSV of entities and answers a `ValidationReport`. Every row is validated first (the shape rules of the slice, applied by `RowValidators` because a CSV row has no schema; a code repeated in the file; a code already in the register) and **when any row fails nothing is registered**: the report says which rows and why, with a `bulk.*` message id per problem, and the file is corrected and sent again. When every row passes, all are registered in one transaction, each with its audit record and its `entity.registered.v1`, through the same rules as a single registration: `EntityRegistrar` now holds the rules and the entity they produce, and both handlers write, audit and publish themselves, because the build's rules keep those in the `@CommandHandler` (and a handler never calls another: the kernel's interceptor claims the request's key around each). The row limit is the configuration item `m1.bulk.max_rows` (seed, 500). **Two things outside M1 had to change:** the kernel's `IdempotencyFilter` read the whole body for its hash and left nothing for the container's multipart parse ("Required part is not present"); for a `multipart/*` request it now hashes the parts and passes the request through. And `EntityRepository` and three accessors of `Entity` are public within `internal`, for the `bulk` package 21A section 3 names. Proof (`BulkRegisterIntegrationTest`, over HTTP as the Federation): a bad row registers nothing and is reported by line and column; a good file registers every row with an audit record and an event each; a code already registered is reported; a missing column and an empty file are refused; an MPCS cannot; forty societies register in a small fraction of the sixty seconds 21A section 11 allows. The screen is M1-12's; the generated web client carries the operation.
- **M1-12 society register screens (24 Sep, branch `feat/m1-12-society-register`).** The first M1 screens, in a `party` web module (`web/src/modules/m1party`): the **society register** (`/party/societies`: status and district go to the server, which filters and pages; the search box narrows what is loaded by code or name as one types; a row leads to the card), the **register form** (`/party/societies/new`: posts `RegisterEntity`, field errors under the fields and the problem title under the form, both as the server translated them; lands on the new card), the **bulk upload** (`/party/societies/bulk`: the CSV of M1-11 goes up as a multipart part, and the `ValidationReport` is a table, one row per line, problems named by field with words from the module catalogue) and the **society card** (`/party/societies/:id`: code, name in the user's language, status chip, facts; Activate for ONBOARDING, Suspend for ACTIVE, Reinstate for SUSPENDED, each only with the permission; Activate is shown disabled WITH the reason while the responsible officer or the VAT number is missing, 21A section 8; Suspend and Reinstate take a reason first). **Three shell components stop being stubs:** `DocumentHeader` (code, title, subtitle, chip, label/value facts with an em dash for "not set", and children for the bar), `ApprovalBar` (a disabled action carries its reason, tied to the button for a screen reader; one primary action) and `ReasonCapture` (code from the codes the screen offers, optional text, confirm or cancel; a dialog where the action was, not a browser modal), with their rules in `shell.css` (tokens only) and the form words `shell.reason.*` in three languages. **The stack:** the dev realm gets `fed-officer` (policy class OWN on the Federation entity, role fed-admin): `fed-admin` holds FEDERATION_VIEW, which sees every entity but may not register, activate or suspend one (21A section 6, the guard `FederationCaller`), so the register needed a user acting for the Federation itself; that is a Keycloak import and needs `make reset`. And a party dev seed (`seed/m1party/entities.dev.sql`) inserts the Federation and the MPCS every other seed and the realm already assume, so a fresh stack has a Federation for the guard to find and one ACTIVE society to show. Proof: 122 web tests (the three components; the actions of the card for every status and permission; every status and bulk problem code has words in three languages), lint, typecheck, `check-permissions`; the Playwright flow `register-society.spec.ts` registers a society as fed-officer, sees the card with Activate disabled and its reason, finds it in the register, is refused the same code again with the server's words, and sees fed-admin refused by the guard. This is part 1 of M1-12 (four of the nine screens of 21A section 8). Not in it: appointing the responsible officer from the card (the `PUT responsible-officer` needs a user of the entity, M1-07), and the screens of relationships, locations, devices, users and roles, which follow their handlers.
- **K-07 documents and numbering base (24 Sep, branch `feat/k-07-documents-and-numbering`).** The document base of doc 18 part C as the kernel schema and the services 19A section 7 names, taken up because M1-05 (locations and till positions: "NumberingService hooks") waits on it and nobody had it. **Schema** (`kernel/V0050`, lane C): `document_type` (reference data, seeded and upserted from `seed/kernel/document-types.yaml`, the seventeen kinds with the 24B flags, names in three languages), `numbering_series` (one counter per series, `UNIQUE NULLS NOT DISTINCT` on type and scope, `fy_reset` pinned false by a check as 24B N-1 decided), `document`, `document_line`, `document_link`, `document_state_history`, `document_attachment`. C-I1 by the trigger of 19A (`document.immutable` as a check violation) plus insert-only triggers on the rows and grants to match; C-I2 by `UNIQUE (series_id, doc_number)` and `kernel.numbering_gaps()`. **Policies:** the template on `numbering_series` and `document` (with `party_read`, and an `own_update` for the counter and for what issuance fills on a stored draft); the rows of a document carry no owner column and get `document_read`/`document_write` on two `SECURITY INVOKER` functions that ask the header under the caller's own policies (a fourth case, added to `RLS_POLICY_TEMPLATE.md`). **Services** (`kernel.api`): `NumberingService` (`registerSeries` idempotent, `holderChange`, `closeSeries`, `activeSeriesOf`; prefix `{entity_code}[-{location_code}[-T{position}]]-{type}`, display `{prefix}-{number:07d}`; audited and published as `series.*.v1`), `DocumentIssuance` (the protocol of doc 18 section 5.5 in the order of 19A: base checks, the type's validator, the number by `UPDATE ... RETURNING`, totals, `content_hash` over a canonical text form a till can reproduce without a JSON library, the rows, the DRAFT to ISSUED history row, `DOCUMENT_ISSUED`, the post-issue hook, `document.issued.v1`), `DocumentTypeHandler` (what a module registers per type it owns: validate, afterIssue, isReversible; a type nobody owns cannot issue), `DocumentLinks` (REVERSES once and only while reversible; SETTLES and CREDITS within the open balance; SUPERSEDES drafts only), `DocumentTypes`. `JdbcDocumentBaseRepository` replaces the 17A in-memory stub. `GapCheck` reads the density function nightly as a federation-wide viewer and logs what it finds. **Two things said out loud for the architect.** (1) The document tables are **not partitioned**: 19A partitions the volume tables by month on `received_at`, but a partitioned header cannot be the target of the plain foreign key on `document_id` that doc 18 gives every module extension table (I-F10) and every link; `received_at` is on every table so that partitioning can be added by a rebuilding migration when the volume asks. (2) The `NUMBERING_GAP` REVIEW audit record and the "unless explained by sync_quarantine" exception wait for K-12 (a scoped system job: an audit record needs an OWN scope, which a nightly job has not) and K-08 (the quarantine table). **Proof** (`DocumentBasePostgresIntegrationTest`, as the application user): registering twice gives one series; a series is registered by its owner only; holder change audited and published; issuance numbers, freezes totals, hashes, stores lines and history, audits and publishes, runs the hook; the hash recomputed from the stored rows equals the stored one; an invalid draft takes no number; a failure after the number leaves neither and the next issuance takes the same number; twenty concurrent issuances from four threads are dense 1 to 20; bilateral without counterparty, unknown and unowned types refused; a closed series issues nothing; an issued header changes only its status (repository, trigger for the superuser, no UPDATE privilege on lines); REVERSES twice and SETTLES beyond the balance refused; SUPERSEDES drafts only; the gap check flags a fabricated gap and nothing on a dense series; the counterparty reads the document and its rows, a stranger nothing, and cannot write a line to it.
- **K-12 scheduled jobs and K-13 business date (24 Sep, branch `feat/k-12-scheduled-jobs`, on top of K-07).** The job runner of 19A section 12 and the business date of section 13, taken together because the day-close trigger is where they meet, and because M1-09 (the expiry job) and the K-07 gap audit wait on the runner. **Schema** (`kernel/V0051`, lane C): `scheduled_job` (the runtime view of what the code registers; `enabled` is the console's switch and the code never writes it), `job_run` (insert-plus-finish history), `shedlock` (the row lock ShedLock takes per run), `location_business_date` (the day-close state of a location, the template policies). **Runner** (`kernel.api.ScheduledJob` on a bean method: name, cron in the business time zone or continuous with a fixed delay, lockTimeout, maxRuntime, critical; the method takes nothing or a `JobExecution` and returns nothing or the items it processed): `JobRegistry` finds every method as its bean is made and upserts the catalogue at start in every role; `JobScheduler` puts them on the clock in the worker role only; `JobRunner` runs one: disabled skips, a lock held by another instance skips, a run past its maximum is interrupted and marked TIMEOUT with a `JOB_TIMED_OUT` ALERT, a failure is FAILED with a `JOB_FAILED` REVIEW record, a critical job is tried once more at once; the lock (ShedLock's JDBC provider on the application data source, database time) outlives the run. **The system scope, said out loud:** a job has no request behind it and so no scope, yet its ALERT must be an audit record. `coop-erp.system.entity-id` names the entity the platform acts as, the Federation (the local stack sets the dev Federation); in its OWN scope a job records what it must, and with the setting empty it logs instead. `SystemScope.federationView()` is the read-only view a fleet-wide job (gap check, cut-off) reads with. The kernel's own jobs moved onto the runner: `partition-maintenance` (daily), `idempotency-expiry` (hourly, as 19A lists it; was daily), `gap-check` (nightly, critical, now recording `NUMBERING_GAP` per gap), `day-close-cutoff`. The outbox relay keeps its own loop: it is K-05's, runs every second, and a lock row per poll is the wrong price; its owner decides. **Business date** (K-13): `LocationBusinessDates` replaces the 17A calendar stub behind `BusinessDate`: a location with no row trades on the calendar date; `DayClose.close` advances it to the day after the one closed, or to today when the location was shut longer, once per calendar day at most, with `DAY_CLOSED` and `location.day_closed.v1`. `DayCloseTrigger` consumes `till_session.closed.v1` and closes the day when the payload says `openSessionsRemaining` is zero (the contract with M6, 26A SessionHook: the kernel reads no module table, so M6 says it in the event); the cut-off job closes every location still behind at 02:30. **Not here:** the manual run from the fleet console (`sys.job.run`, MFA: K-02/K-03), the per-location cut-off (a configuration item, K-11), the till clock offset (`ClockOffset`, with K-08 heartbeats). **Proof** (`JobRunnerPostgresIntegrationTest`, `DayClosePostgresIntegrationTest`): the code registers its jobs and leaves the switch alone; a run is recorded with what it processed; two instances make one run per schedule and the next schedule runs again; a run past its maximum is interrupted, TIMEOUT, alerted under the system entity; a failure is FAILED and a critical job retried once, with the REVIEW records; a location trades on the calendar date until its first close; a close advances once per day and tells everybody; a location shut for a week reopens on today; the cut-off closes every location behind; the trigger is bound to the session event.
- **K-11 configuration register (25 Sep, branch `feat/k-11-configuration-register`, on top of K-12).** The register of doc 19 section 8 as 19A section 11 restates it. **Schema** (`kernel/V0052`, lane C): `config_item` (reference data: type, schema, descriptions in three languages, default, the finest scope allowed, the change permission, sensitive, till_visible, owning module) and `config_value` (append-only; a unique expression index where 19A wrote a primary key with `coalesce()`, which PostgreSQL does not take). The value policies widen the template's `own_read` on purpose: a caller reads the federation-wide rows (nobody's), its entity's rows and, at a location, that location's rows and the entity-wide ones, because a read is a resolution. **Register** (`seed/kernel/config-items.yaml`, upserted on every start as the migrator, items never values): the business time zone, the day-close cut-off, `m1.bulk.max_rows`, `trade.federation_direct.enabled`, `security.policy.mfa_enabled` (sensitive), `till.idle_lock` (till-visible); the doc 07 section 4 register is not in the repository's design texts, so the items are the ones the code and the module seeds name today, and the file is the one place to grow. A module's `seed/<module>/config.yaml` still goes through `ConfigSeeder.addDefaults`: a registered key gets its default, an unknown key becomes a plain ENTITY-scoped string item of that module. **Resolver** (`JdbcConfigRegistry` replaces the 17A stub behind `ConfigRegistry`): location, then entity, then federation, then the default; typed getters (`getDuration` added); a Caffeine cache per instance, emptied for a key by the change on the same instance, by `config.changed.v1` on the others (`ConfigCacheInvalidator`, where the consumer runtime runs), and by a thirty-second expiry everywhere. **Setter** (`ConfigRegistry.set`): the item must exist; the value must parse in the item's type and pass its schema (minimum, maximum, enum, pattern, max_length: a small validator, no JSON Schema library until an item needs one); the scope must be one the item allows; a federation-wide value is the Federation's alone (the system entity of K-12); a sensitive item needs a second factor fresher than ten minutes (`mfa.required`; K-02 refines the freshness); then the append-only row, `CONFIG_CHANGED` with before and after, the event. **Waiting on others, said out loud:** the item's `change_permission` is checked when K-03b gives the kernel a permission resolver (the calling handler carries the permission until then); the change-log fan-out for till-visible keys needs K-08's change log (the event carries the flag). **Proof** (`ConfigRegistryPostgresIntegrationTest`): the register is seeded and the default answers; the most specific scope wins and another entity is untouched; a change is appended, audited, published and seen at once; a value outside the schema, of the wrong type or of an unknown key is refused with nothing recorded; the scope must be one the item allows and the entity the caller's own; a sensitive item needs a fresh second factor; a module default updates a registered item and registers an unknown one; the M1 tests (bulk registration reads `m1.bulk.max_rows` from the register now) still pass.
- **K-09 attachments (25 Sep, branch `feat/k-09-attachments`, on top of K-11).** The presign and the verifier of 19A section 9 on the `document_attachment` table K-07 created. **API** (`kernel.api.Attachments`): `presignUpload(documentId, attachmentId, contentType, sha256Hex, ctx)` records the attachment PENDING under the document's policies (a document the caller cannot see is "not found", one it can see but does not own is refused by the row policy) and returns a pre-signed PUT of fifteen minutes for the key `attachments/{entity}/{documentId}/{attachmentId}`, audited `ATTACHMENT_PRESIGNED`; `presignDownload` gives a pre-signed GET only for a COMPLETE attachment of a document the caller can read (the document's RLS is the scope check); `status`. **Store**: an `ObjectStore` boundary (presign PUT, presign GET, HEAD, SHA-256) with `S3ObjectStore` behind it on the AWS SDK v2 S3 client, path-style, provider-neutral; two endpoints, the one the backend reaches and the public one a browser or a till reaches with the URL (compose: `minio:9000` and `localhost:9000`), settings under `coop-erp.object-store`. **Verifier** (`attachment-verify`, a K-12 job every five minutes): reads PENDING rows as a federation-wide viewer and, in the OWN scope of each document's entity, COMPLETEs an object whose hash matches what the client announced (or takes the store's hash when nothing was announced) with `ATTACHMENT_COMPLETED` and `attachment.completed.v1`, FAILs a mismatch or an object not uploaded inside the upload window (24 h) with an `ATTACHMENT_FAILED` REVIEW record. **Proof**: `AttachmentsPostgresIntegrationTest` with an in-memory store (presign records PENDING on the caller's own document; a stranger cannot attach and the counterparty cannot either; the verifier completes an uploaded object with the right hash and the owner and the counterparty then get a URL, a stranger none; a wrong hash and an object that never came are FAILED; a malformed hash is refused) and `S3ObjectStoreIntegrationTest` against a MinIO container (a pre-signed PUT uploads; HEAD, hash and a pre-signed GET agree; a missing key is absent). **Not here:** the sync endpoint `POST /sync/v1/attachments/presign` (K-08) and the image thumbnails (M2-11).
- **K-06 internationalisation, part 1 (25 Sep, branch `feat/k-06-i18n`, on top of K-09).** Doc 19 section 5 as 19A section 6 restates it, without the A4 renderer (part 2, below). **Messages**: `IcuMessages` replaces the 17A stub on `java.text.MessageFormat` with ICU MessageFormat, the same three catalogues, positional arguments as before and now plurals, selects and named arguments (one Map); numbers inside a message in Western Arabic digits in every language (DR-6, the LATN numbering system on the format's locale). `Messages.text` returns `Text(value, fallback)`: an id the requested language lacks comes in English with the flag, and `ProblemResponses` puts `"fallback": true` on the problem document so the web shell can show its EN mark; a language the catalogue does not have (German) gets English without the flag, because that is not a missing translation; an id no language has comes back as the id and is logged. **Normalisation**: `TextNormaliser.nfc` (NFC and strip) and `NfcConverter`, a JPA converter applied to every String attribute automatically, so the write boundary is NFC without a module doing anything; a handler that writes through JDBC calls the normaliser. **Formats**: `Formats` (`IcuFormats`): money `1,234.00` and `Rs 1,234.00` (`රු.` and `ரூ.` for the other two), quantities to three decimals, `dd/MM/yyyy`, `dd/MM/yyyy HH:mm` in the business zone, LATN digits whatever the locale. **Collation and search**: `SearchSupport.orderByDisplayLanguage(column, locale)` gives the ORDER BY on the display language's column and ICU collation with English as the tie breaker; `matchAnyLanguage` and `pattern` give the three-column ILIKE the trigram indexes serve, NFC and escaped. **Test set**: `i18n/sort-test-set.yaml`, the curated Sinhala, Tamil and English names with their known order and the equivalent-spelling pairs, run on every build against the baseline collations (`CollationPostgresIntegrationTest`), meant to grow with the steward's findings. **Proof**: `IcuMessagesTest` (language, fallback with and without the mark, plurals, named arguments, LATN digits, broken and missing files stop the start), `FormatsAndSearchTest`, `CollationPostgresIntegrationTest` (the set sorts as listed under `kernel.si_icu`, `ta_icu`, `en_icu`; equivalent spellings are one name once normalised; text posted through the hello handler reaches the table in NFC). **Part 2, for the architect**: the A4 renderer (Thymeleaf to PDF through a headless Chromium worker with bundled Noto fonts, DR-3) needs two decisions before code: which font files are bundled and where (the web client takes them from npm; the backend has none), and whether the worker image carries Chromium (the pipeline installs it for the web e2e only). Until then every printed document is M4's to render.
- **CR-17A-3 applied to every table (25 Sep, branch `feat/cr-17a-3-own-policies`, on top of K-06).** K-01's close-out found forty `own_*` policies on main without the class test; on inspection only three tables were left unguarded once the multi-line policies of M1 were read whole: `kernel.audit_event` (the parent and every monthly partition, and the partition function that makes new ones), `kernel.event_outbox`'s insert policy (parent, partitions, function) and `pricing.price_list`. `kernel/V0053` alters the ledger policies in place and replaces the two partition functions so that they drop and recreate the `own_*` text on every run, which also corrects a partition made by the old text the next night; `m3pricing/V0002` alters the price list's like hello's V0002 did. **Kept for good**: `OwnPoliciesTestTheClassIntegrationTest` reads `pg_policies` after every migration has run and fails on any `own_*` policy, or the outbox insert, in any schema whose text lacks `kernel.scope_class() = OWN`, partitions included. M1's policies already had the test and pass it unchanged.
- **K-10 notifications delivery, part 1 (25 Sep, branch `feat/k-10-notifications`, on top of CR-17A-3).** Doc 19 section 7 as 19A section 10 restates it: the kernel delivers, M9 owns what is said and through which provider. **What M9 implements** (`kernel.api`): `NotificationChannel` (one bean per channel, SMS, EMAIL, IN_APP: send, return the provider reference or throw), `NotificationRuleQueries` (the active rules for an event type and the templates in three languages) and `NotificationAudience` (the role kinds from M1, the CUSTOMER kind from M7 with the STATEMENTS consent); without those beans the dispatcher matches nothing and only direct sends work. **What the kernel does**: `Notifications.send` for the few direct cases (a reset code, a lockout notice), and `NotificationDispatcher`, an event consumer on every type (`@EventConsumer(types = "*")`, which the registry, the topic binding `#` and the replayer now understand; a wildcard consumer is handed the envelope with the payload inside) that matches M9's rules by type and predicate, resolves the EXPLICIT audience from the payload and the rest through the audience beans, and hands each recipient to `NotificationService`: a `notification_log` row QUEUED (`kernel/V0054`; the unique key rule, event, recipient is the idempotency, so a replayed event sends once), the suppressions from the register (the entity's kill switch `notification.<channel>.enabled`, the quiet hours `notification.sms.quiet_hours`, the same template to the same person inside an hour), rendering in the recipient's language with English fallback (`NotificationRenderer`: an M9 template through ICU MessageFormat, or a catalogue message id), the channel, then SENT with the provider reference and the rendered length, or a failed attempt with backoff (one, five, fifteen minutes) that the sweep `notification-retry` (every two minutes, a K-12 job) retries, three attempts inside 24 hours, then FAILED with a `NOTIFICATION_FAILED` ALERT. **Privacy** (ADR-27): the log holds a hash of channel and recipient and the body's length, never the number or the text; the recipient of a retry is held in memory on the instance that queued it, and a row that instance no longer holds is FAILED with a clear error rather than stored. **Not here**: the secondary provider after the third attempt (M9's `provider_config`), the customer consent check (M7), the SMS length rule at authoring (M9). **Proof** (`NotificationsPostgresIntegrationTest` with a test channel and a test rule store): a direct send is rendered in the recipient's language and logged without the number; a language without the text falls back to English; the same thing to the same person sends once, and a second inside the hour is SUPPRESSED; the kill switch and the quiet hours suppress with their reason; a failing provider is retried by the sweep on its back-off and given up with the ALERT after three; the dispatcher matches an event against a rule and its explicit audience, sends once on replay, and skips a predicate that does not hold; an unknown channel or no recipient is refused.
- **K-03b permission resolver, interceptor order, separation of duties (25 Sep, branch `feat/k-03b-permission-resolver`, on top of K-10).** 19A section 3. `JdbcPermissionResolver` reads M1's tables under the caller's own row-level security: the union of the permissions of every ACTIVE role the user is assigned in the scope, an entity-wide assignment applying at every location and a location assignment there alone; only the OWN class resolves to anything. Cached per instance under (user, entity, location) for ten minutes, emptied by `role.changed.v1`, `role.assigned.v1`, `role.revoked.v1` and `user.deactivated.v1` (`PermissionCacheInvalidator`, M1's events of 21A). `JdbcSod.assertDistinct` refuses the same person on an INSTANCE pair (`sod.same_person`), federation-wide or the entity's. **The interceptor** now runs in the order 19A fixes: permission, then MFA (a permission the catalogue flags needs a second factor fresher than ten minutes), then idempotency, then the handler; `permission.denied` is 403. **Enforcement is a switch, off by default, said out loud:** `coop-erp.security.enforce-permissions`. The development stub still takes the user from a header and the dev realm's users hold no roles in the security schema, so enforcing today would refuse every command on every developer machine; until K-02 puts the token's user behind a request, a would-be refusal is logged (`Would refuse ...`) so the gap between the roles and the handlers shows first. **One M1 policy corrected on the way** (`m1security/V0008`, for the M1 owner to know): `own_read` on `security.user_role` hid an entity-wide assignment (no location) from a location-scoped session, so a shop user lost the entity-wide roles that apply at the till; the policy now shows a location-scoped session its location's assignments and the entity-wide ones. **Not here**: `limits` and `Resolved` (19A names them, no document defines them), the till snapshot signer (K-08), the guardrails at role authoring (M1-08 with kernel helpers). **Proof**: `PermissionResolverPostgresIntegrationTest` (the union over roles and nothing more, another entity nothing; entity assignment everywhere and location assignment there alone; a read-only class nothing; the cache empties on the role events; the catalogue's MFA flag; the INSTANCE pair refuses the same person and admits another) and `PermissionEnforcementPostgresIntegrationTest` (enforcement on for its context: a hello registration is refused 403 `permission.denied` without the permission and runs with it). The hello and M1 tests pass unchanged with enforcement off.
- **K-02 identity, first pull request (25 Sep, branch `feat/k-02-identity`, on top of K-03b).** 19A section 2, the token half. **Resource server** (`kernel.internal.security.SecurityConfig`): Spring Security's OAuth2 resource server verifies every bearer token against the provider's JWKS, its issuer and its times; the decoder is built from `coop-erp.security.oidc.issuer` and `.jwk-set-uri`, not from the issuer's discovery document, because inside compose the backend reaches the provider at another address (`keycloak:8080`) than the browser does (`localhost:8085`). A refused token is a 401 problem document `token.invalid` with `WWW-Authenticate` (`ProblemAuthenticationEntryPoint`); health and metrics need no token; no session, no CSRF. **`JwtClaimsMapper`** turns the claims of doc 19 section 1 into the `ScopeContext`: `sub` (a UUID: the platform issues the ids the provider carries), `dev`, `ent`, `scopes` ("entity" or "entity/location"; absent, the home entity alone, entity-wide), `cls` (absent or unknown, NONE), `grants`, `mfa_at` (epoch seconds), `lang` (then Accept-Language, then English); the active scope is the X-Scope-Entity and X-Scope-Location headers, validated by the scope filter against the token's scopes. **`TokenCurrentScope`** replaces `DevCurrentScope`; the scope filter now runs after Spring Security's chain. **The command interceptor** takes the user from the scope and no longer from a header: a command with no user is `scope.required`; a stale second factor is now 401 `mfa.required` with the provider's step-up address (`coop-erp.security.oidc.step-up-url`) in the problem's params, for the web shell's retry (17A section 7). **`PinHasher`** (`kernel.api`, `Argon2PinHasher`): Argon2id, 64 MB, three iterations, parallelism two, 16-byte salt, 32-byte hash, as a PHC string. **`IdentityProviderClient`** declared in `kernel.api` with CR-19A-1's signature; its implementation is the second pull request. **The dev realm** gives its four users fixed ids, so the token's `sub` is the platform's user id, and `seed/m1security/users.dev.sql` gives each a development role with every permission of the catalogue in its home entity; **compose now enforces permissions** (`COOP_ERP_ENFORCE_PERMISSIONS`, default true there, off elsewhere), and the smoke test registers as the seeded MPCS administrator. **Until the second pull request** a request without a token still names its user in the development headers (`coop-erp.security.dev-headers`, true by default, to be deleted with the headers on both sides, `DevScopeContextProvider` and `DevStubsGuard` with them); a request without either gets no user and no scope. **Not here**: the provider mapper endpoint that puts `scopes` and `roles` into the token from M1's assignments, `KeycloakAdminClient`, device authentication (`DeviceAuth`, `DeviceCredentials`), the web shell's step-up round trip, the deletion of the headers. **Proof**: `TokenAuthenticationPostgresIntegrationTest` (a provider of its own: a key pair, a JWKS on a local socket, tokens signed in the test, permissions enforced; a valid token runs the command as the token's user and another user is refused 403; the token wins over the development header; a token signed by another key, an expired one, one from another issuer and one that is not a JWT are 401 `token.invalid` as problem documents; without a token the development headers still name the user and without either the command is `scope.required`; health needs no token), `JwtClaimsMapperTest` (every claim of the table; the home entity as the only scope; the header's choice among several; unknown class NONE; the language order; the correlation header; a subject that is not a user id refused), `Argon2PinHasherTest`. `make e2e` (the browser signs in for real and the backend verifies the token) is the net for the local stack.
- **M1-05 locations and till positions (25 Sep, branch `feat/m1-05-locations-and-positions`).** The location and position commands of 21A section 6 on the K-07 numbering service. **Commands** (`m1party/internal/location`, one `@CommandHandler` each, audit and event in the transaction): RegisterLocation (owner = the caller's entity, acting entity-wide; owner ACTIVE or ONBOARDING; code unique in the owner; PLANNED; the language defaults to the entity's), UpdateLocation (names, address, district, map position, language, trading hours, size band; code and type never change, the code being part of every document number), ConfirmLocationConnectivity (the gate of doc 21 section 3.3, once, the audit record says who), StartLocationOnboarding, ActivateLocation (connectivity met; for a SHOP a primary till and at least one operator, a TILL or BOTH user with an assignment at the shop), MarkLocationDormant (reason), ReactivateLocation (connectivity met), SetPrimaryTill (MFA; position of this location, ACTIVE; reason), RegisterTillPosition (SHOP only; number unused, retired ones included), RetireTillPosition (no device at it). **NumberingService hooks** (`SeriesHooks`, which types from the kernel's document type registry): a shop gets its LOCATION series (CNT, GRN, RPK, WOF, XFR, prefix `M905-S01-GRN`) on registration, a warehouse or an office none (their types number from the entity's series); a position gets its TILL_POSITION series (CPR, RCT, prefix `M905-S01-T1-RCT`) in the same transaction, which is the "done when" of the ticket; SetPrimaryTill moves the location series' counters to the device at the new primary till (`holderChange`, `series.holder_changed.v1` from the kernel) and moves nothing while no device is there; retiring a position closes its series. **Queries** (`PartyQueries`): getLocation, listLocations (status, type, cursor), listTillPositions (with the primary flag), all under row-level security only. **Schema** (`m1party/V0005`): V0001 already had both tables and their OWN and FEDERATION_VIEW policies; added are a composite key so a position's owner is its shop's owner, a composite key so the primary till is a position of the same location, and `ext_view` on both tables. **Slice**: thirteen operations under tag `Locations`; web client regenerated. **Events**: `location.registered/updated/onboarding/activated/dormant/primary_changed.v1`, `till_position.registered/retired.v1`, the location named `registeredLocationId`, `activatedLocationId` ... as the kernel's `LocationDayClosed` does, because the outbox reads `locationId` as the place of an event. Ten audit types, twenty message ids in three languages. **Decisions to note:** 21A section 3.3's `prt.location.register`, `.activate` and `.primary` are added to the catalogue next to `prt.location.manage`/`.view` (whose removal is `CR-21A-1` item 1's); update uses `.register`, and onboarding, dormant, reactivate and the connectivity gate use `.activate`, since doc 21's `prt.location.onboard` and `.dormant` are not in 21A's catalogue; UpdateLocation, ConfirmLocationConnectivity and `location.updated.v1` are not named by 21A or doc 21 section 5.3 and are added so that hours, language and the gate can be set at all; RetireTillPosition refuses the shop's primary till (the location series would be held at a position that no longer exists); the operator and primary-till gates apply to a SHOP only. The dev seed (`seed/m1party/locations.dev.sql`) adds shop S01 (PLANNED) and warehouse W01 (ACTIVE) to the development MPCS, and no positions: a position's series are in the kernel's schema, which an M1 seed may not write, so positions are registered through the API. **Not here:** "no open till session" for MarkDormant and RetireTillPosition (an M6 query; M6 does not exist), "device staged" for StartOnboarding (M1-06 and doc 31), device assignment and the counter transfer on it (M1-06), the location part of the till snapshot (K-08, M1-10), PARTY's ship-to names (a projection, with M4's drops), the permission and MFA checks themselves (K-03b). **Proof** (`LocationsPostgresIntegrationTest`, 19 cases as the application user, and `TradingHoursTest`): a shop is registered PLANNED with its five location series, audited and published; a warehouse gets no series; every guard of RegisterLocation, RegisterTillPosition, SetPrimaryTill and RetireTillPosition refuses with its message id and commits nothing; a position is registered with its two till series in the same transaction; the life cycle goes PLANNED, ONBOARDING, ACTIVE, DORMANT, ACTIVE only through its gates; naming the primary till moves the five counters to its device, or none when there is no device; retiring closes the series and the number is never reused; the database refuses a primary till of another location; a shop-scoped caller sees and changes its own shop only and still registers a till there; the Federation view reads every location and writes none; an external grant reads its entities' locations; over HTTP a shop and a till are registered, a malformed code is a 400 and a repeated till number a 422 with its code.
- **M1-09 external grants (25 Sep, branch `feat/m1-09-external-grants`).** The time-boxed read-only access of a regulator or auditor, doc 21 section 4.6 and flow 6.5, as 21A section 6 specifies it. **Commands** (`m1party/internal/grant`, `POST /v1/security/external-grants` and `POST /v1/security/external-grants/{grantId}/revoke`, both `gov.external.grant` with MFA; `GET /v1/security/external-grants` is the Federation's register, active, expired and revoked): GrantExternalView guards the Federation as the caller (entity-wide OWN, the `FederationCaller` guard with its own message id), a grantee the Federation can see, of kind EXTERNAL, not deactivated, every entity of the scope existing, the window, and a reason, then one ACTIVE row owned by the Federation, `EXTERNAL_GRANT_ISSUED` and `external_grant.issued.v1`; RevokeExternalView guards an ACTIVE grant whose window has not passed and a reason, then REVOKED, `EXTERNAL_GRANT_REVOKED`, `external_grant.revoked.v1`; ExpireExternalGrant (no HTTP; the job's command) guards ACTIVE and due, then EXPIRED, `EXTERNAL_GRANT_EXPIRED`, `external_grant.expired.v1`. **The window** (`GrantWindow`, a pure function with its own unit test): a missing or past start is now, a future start is kept, the end must be after the start, and no later than `m1.external_grant.max_months` after it (a new FEDERATION item in the register, 1 to 12, default 12, sensitive; assumes doc 10 L-07); the months are added by the database, as the table's CHECK constraint adds them in the session's time zone, so that a grant the guard admits is never refused by the constraint on the last day of February. **Scope resolution for EXTERNAL** (as corrected on 26 Sep, branch `fix/m1-roles-users-grants-review`): the kernel's `JdbcUserScopes.grantsOf` (K-02) is the one path a request takes; the M1 query `activeGrantedEntities` this ticket added was never called outside its tests and is deleted, and the policy `grantee_read` (V0009) stays in the schema unread. The kernel reads the ACTIVE grants of an ACTIVE grantee whose window contains now, caches them a minute at most and never past the earliest end, and forgets them on the grant and user events, so a revoked grant admits nothing at once on the revoking instance and an ended one from the second it ends on every instance, whether or not the job has run. **Expiry job** `external-grant-expiry` (K-12 `@ScheduledJob`, hourly at five past, `coop-erp.m1.external-grant-expiry-cron`): in the system scope, every ACTIVE grant past its end through the handler, one transaction each; without a system entity it logs and changes nothing. **What an EXTERNAL caller reads** (`m1security/V0009`): `ext_view` on `security.role`, `role_permission`, `user_role` and `sod_pair` for the granted entities, read only; `security.app_user` is left out on purpose (it carries `pin_hash` and `provider_subject`, which RLS cannot hide by column). **Not here:** the token mapper putting the grants into the token (K-02 part 2): the kernel cannot import a module, so it will need a kernel-side interface M1 implements, or the mapper calls this query; `ext_view` on `party.entity` (the other `party` tables have it from their own migrations: `location` and `till_position` in m1party V0005, `entity_relationship` in V0007, `device` in V0009; corrected on 26 Sep); login and export auditing and export volume on the exception queue (K-02, K-10); the grants screen (M1-12). **Proof:** `ExternalGrantsIntegrationTest` runs flow 6.5 through HTTP (grant, resolution to the one society, the external session reads that society's security rows and nothing of the other and writes nothing, the register lists it, a thirteen-month grant refused with nothing recorded, revocation immediate, an ended grant admitting nothing before the job and marked, audited and announced by it) and every guard of the three commands with nothing committed; `GrantWindowTest` every case of the window.
- **M1-04 trading relationships (25 Sep, branch `feat/m1-04-relationships`).** The relationship aggregate of 21A sections 3.1 and 6 (doc 21 sections 3.2 and 4.2), done when scenarios 6.2 and 6.4 of doc 21 pass. **What V0001 already held and what was added:** the table, its two checks, the A-I3 exclusion constraint (one ACTIVE row per seller and buyer per date) and the OWN and Federation-view policies were there; `m1party/V0007` adds `party_read` in the template's form (the buyer reads its side in OWN scope as well as PARTY; V0004 admitted PARTY only), `ext_view`, a check on `allocation_rule` (FCFS, PRO_RATA, QUOTA, 24A section 6.2), the pair and buyer indexes of 21A section 7, and `party.trading_standing(entity)`, a narrow SECURITY DEFINER function that tells an OWN caller the type and status of the counterparty (the seller may not read the buyer's entity row) with a read policy `TO app_seed` on `party.entity` for the function's owner. **Handlers** (`internal/relationship`): `OpenTradingRelationship` (seller is the caller's entity in an entity-wide OWN scope; seller is not the buyer; both ACTIVE or ONBOARDING; the tier rule, Federation to distributor and distributor to society, and Federation to society only with `trade.federation_direct.enabled`, doc 10 F-03; no overlapping ACTIVE row; DRAFT), `ActivateRelationship` (seller; DRAFT; a price list and payment terms; M3's check; no overlap; ACTIVE), `AmendRelationshipTerms` (21A section 6.1: ACTIVE; seller; new terms start after the current row's first day and before its end; a limit change needs `bil.creditlimit.change` and a second factor younger than `m1.relationship.credit_limit_mfa_max_age`; a reason; something changes; no overlap; the current row is closed the day before and never otherwise edited, the next row inserted; `RELATIONSHIP_AMENDED` and, for a limit, `CREDIT_LIMIT_CHANGED`, with `relationship.amended.v1` and `credit_limit.changed.v1`), `SuspendRelationship` (seller; ACTIVE; reason; MFA on the annotation). The pre-check names the row in the way; SQLSTATE 23P01 from the constraint becomes the same `m1.relationship.overlap` when two clerks race. **Published:** `query.RelationshipQueries` (`lookupRelationship(seller, buyer, date)` for M3 and M4, `getRelationship`, `listRelationships(side)`); the five events; `api.TradePriceListCheck`, the M3 question of ActivateRelationship, declared in M1 because M3 depends on M1, with the stub `AcceptingPriceListCheck` (21A section 11: accepts every list, logs at WARN; deleted when M3 implements the interface). **Slice:** six operations under `/v1/party/relationships` (open, list, get, activate, amend answering the new row, suspend); web client regenerated. **Seeds:** the four verb codes of 21A section 3.3 and `bil.creditlimit.change` in the catalogue and in the Entity Administrator template; five audit types; the MFA age item; a dev distributor `D001` and the chain Federation to D001 to M001 (`relationships.dev.sql`). **Decisions:** 422 for an overlap, not 409 (`CR-21A-1` item 3); message ids `m1.relationship.*`; the credit-limit permission is a handler guard and is skipped while no `PermissionResolver` bean exists (K-03b not on `main`), the second factor is enforced; the coarse `prt.relationship.manage` stays unused until the catalogue freeze. **Not here:** the agreement-sheet screen (M1-12), a reactivation of a SUSPENDED relationship (no transition in doc 21), the change-log fan-out of relationship events to tills (K-08). **Proof:** `RelationshipHandlersTest` (every guard with its message id, the amendment in full, the race backstop, month-end closure arithmetic, the terms hash); `RelationshipScenariosIntegrationTest` (6.2: open, activate, M3 asked, buyer and Federation view look the terms up, overlap refused by the pre-check and by the constraint, unpublished list refused with M3's reason, tier rule; 6.4: limit raised from 1 July, old row closed 30 June with its terms intact, both audits and events, lookup by date; missing reason and stale MFA refused with nothing committed; suspend; forty random ranges never escape the pre-check; the six operations over HTTP); `RelationshipRlsIntegrationTest` (seller, buyer in OWN and PARTY, stranger, Federation view, regulator with and without grant, NONE; the buyer cannot write; the exclusion constraint; the standing function).
- **K-02 identity, second pull request (25 Sep, branch `feat/k-02-identity-2`, on top of the first).** The development headers are gone on both sides: every request under `/v1` needs a token (401 `auth.required` without one, `token.invalid` for one the resource server refuses); `DevScopeContextProvider` and `DevStubsGuard` are deleted, the web client sends the bearer token and `X-Scope-Entity` (the caller's choice of active scope, which stays), the smoke test signs in with the password grant. **Scopes from the platform's records**: 19A section 2 puts `scopes` and `roles` into the token through a provider mapper calling a kernel endpoint; our provider has no such mapper without a provider-specific extension, so `JdbcUserScopes` resolves the same facts at request time from M1's `user_role` (and `external_grant` for an EXTERNAL_TIMEBOXED caller) as the federation-wide viewer, cached ten minutes and emptied by the same events as the permission cache; a token that carries `scopes` or `grants` is honoured as it is; the scopes are the home entity (entity-wide) plus the assignments. **`IdentityProviderClient` implemented** as `KeycloakAdminClient` over the provider's plain administration REST API, no provider library: create a login (the platform's user id travels as the `uid` attribute and claim, read before `sub`), disable, one-time password, second-factor reset, end sessions; the scope rule of ADR-18 (the caller acts in the OWN class of the target's home entity, else `identity.scope`) runs before every call; the service account is a confidential client with `manage-users` and nothing else; every method takes the caller's `ScopeContext` (a departure from CR-19A-1's proposal, which had no caller). **Step-up in the web shell**: 401 `mfa.required` sends the user to the identity server with `prompt=login` and back to the same page. **The dev realm** declares the platform's user attributes in its user profile (Keycloak 24+ keeps only declared attributes written through the administration API) and carries the service account. **Not here**: device authentication (needs M1-06's devices), the provider mapper endpoint (replaced by the request-time resolution; the architect may want the claims in the token instead). **Proof**: `TokenAuthenticationPostgresIntegrationTest` (as before, plus: a token without a home entity acts where the assignments put the user; no token is 401 `auth.required`), `JwtClaimsMapperTest` (uid before sub, records plus home entity, an external caller's grants), `KeycloakAdminClientIntegrationTest` (against the provider of the local stack started with the same realm file: the scope rule, create, a taken name, one-time password, TOTP reset, sessions, disable, as the realm administrator then sees it). Every HTTP test of the repository now signs its requests through `TestIdentityProvider` in the shared test base, so the claims mapper and the scope filter run on every request of every module test.
- **M2-01 catalogue scaffold, schema and seeds (25 Sep, branch `feat/m2-01-catalogue-scaffold`).** The first M2 ticket (22A section 10: "Migrations and seeds apply"). `make new-module NAME=m2catalogue SCHEMA=catalogue ENTITY=sku` was run, and its greeting-shaped copy (entity, handler, queries, controller, migration, dev seed, integration test, web page, message ids) removed again: hello keeps the tests that prove the template, and nothing of the copy was needed. The schema ownership was already in place (`m2catalogue` owns `catalogue` in `new-module.mjs`, which `check-schema-ownership.mjs`, `ArchitectureTests` and `FlywayConfig` agree with). **Schema** (`m2catalogue/V0001`, a new lane): `uom`, `tax_category`, `tax_rate`, `sku`, `sku_uom_conversion`, `sku_barcode`, `tag`, `sku_tag` and `batch` as 22A section 3 writes them, with the exclusion constraints on conversions and rates, the trigram indexes, B-I1 and B-I2; `batch` partitioned by month of `created_at` with `catalogue.ensure_batch_partitions(n)` (partitions get RLS forced and the parent's policies copied; V0001 creates this month and three more). Every table carries the RLS template (own_read/own_write on class OWN, fed_view, ext_view) plus `own_update` where it is updated, and the reference reads 22A asks for: `authenticated_read` (units, tax), `shared_read` (a SHARED SKU and its conversions, barcodes and tags; every batch), `governed_read` (governed tags); `seed_reference TO app_seed` on the seeded tables. Batch: INSERT and UPDATE(status) only. **Seeds**: `M2SeedLoader` (as the migrator, like `M1SeedLoader`) loads `uom.yaml` (10 units), `tags.yaml` (8 governed tags) and `tax.yaml` (STD 18 %, ZERO, EXEMPT, owned by the Federation from `coop-erp.system.entity-id`; skipped with a warning when it is not set), insert-if-absent; `audit-event-types.yaml` carries the 19 audit codes of 22A section 6. **Permissions**: M1's loader reads one file and `security.permission` is M1's, so the eleven `cat.*` codes are appended to `seed/m1party/permissions.yaml` as 22A section 3.1 says. **Slice** `m2catalogue.yaml`: info only, no operation (M2-02 adds the first). **Web**: the registration the scaffolder makes, with no route and no navigation entry. **Decisions**: the tables of later tickets (supplier, sku_image, sku_alias, duplicate_suspect, location_assortment) come with those tickets; collations and `gin_trgm_ops` qualified `kernel.`; the batch key is a unique index (22A's constraint holds an expression; it includes `created_at`, so RegisterBatch must look up first); `shared_read` tests the class `<> 'NONE'`; the kernel's `PartitionJob` takes no list of tables, so nothing calls the batch partition function daily yet, and M2-05 must settle it (a module job, or a registration hook in the kernel). **Not here**: any command, query or operation; the transliteration table (M2-08). **Proof**: `CatalogueSchemaIntegrationTest` (the nine tables, forced RLS and the exact policy set on every table and partition, own_* test the class, grants, partitions), `CatalogueRlsIntegrationTest` (12 rows of the matrix: OWN, SHARED, FEDERATION_VIEW, EXTERNAL, NONE, barcodes of shared items, batch read by all and changed by its registrant, printed MRP never edited, no unit written by the application), `M2SeedLoaderTest` (counts, translations, Federation ownership, a second run inserts nothing and leaves edits and later rates alone), `M1SeedLoaderTest` (28 permissions). `m2catalogue/README.md` is the living guide and lists what M2-02 to M2-09 build on.
- **K-02 identity, second pull request (25 Sep, branch `feat/k-02-identity-2`, on top of the first).** The development headers are gone on both sides: every request under `/v1` needs a token (401 `auth.required` without one, `token.invalid` for one the resource server refuses); `DevScopeContextProvider` and `DevStubsGuard` are deleted, the web client sends the bearer token and `X-Scope-Entity` (the caller's choice of active scope, which stays), the smoke test signs in with the password grant. **Scopes from the platform's records**: 19A section 2 puts `scopes` and `roles` into the token through a provider mapper calling a kernel endpoint; our provider has no such mapper without a provider-specific extension, so `JdbcUserScopes` resolves the same facts at request time from M1's `user_role` (and `external_grant` for an EXTERNAL_TIMEBOXED caller) as the federation-wide viewer, cached ten minutes and emptied by the same events as the permission cache; a token that carries `scopes` or `grants` is honoured as it is; the scopes are the home entity (entity-wide) plus the assignments. **`IdentityProviderClient` implemented** as `KeycloakAdminClient` over the provider's plain administration REST API, no provider library: create a login (the platform's user id travels as the `uid` attribute and claim, read before `sub`), disable, one-time password, second-factor reset, end sessions; the scope rule of ADR-18 (the caller acts in the OWN class of the target's home entity, else `identity.scope`) runs before every call; the service account is a confidential client with `manage-users` and nothing else; every method takes the caller's `ScopeContext` (a departure from CR-19A-1's proposal, which had no caller). **Step-up in the web shell**: 401 `mfa.required` sends the user to the identity server with `prompt=login` and back to the same page. **The dev realm** declares the platform's user attributes in its user profile (Keycloak 24+ keeps only declared attributes written through the administration API) and carries the service account. **Not here**: device authentication (needs M1-06's devices), the provider mapper endpoint (replaced by the request-time resolution; the architect may want the claims in the token instead). **Proof**: `TokenAuthenticationPostgresIntegrationTest` (as before, plus: a token without a home entity acts where the assignments put the user; no token is 401 `auth.required`), `JwtClaimsMapperTest` (uid before sub, records plus home entity, an external caller's grants), `KeycloakAdminClientIntegrationTest` (against the provider of the local stack started with the same realm file: the scope rule, create, a taken name, one-time password, TOTP reset, sessions, disable, as the realm administrator then sees it). Every HTTP test of the repository now signs its requests through `TestIdentityProvider` in the shared test base, so the claims mapper and the scope filter run on every request of every module test.
- **M1-08 roles, assignments, separation-of-duties pairs (25 Sep, branch `feat/m1-08-roles-and-sod`, on top of K-02's second pull request).** 21A sections 5 and 6 for role, role_permission, user_role and sod_pair; done when scenario 6.3 and the property tests pass. **Commands** (`m1party.api`, handlers in `internal/security/role`, all `gov.role.manage` with MFA): CreateRole (from a template or from scratch; a clone keeps `template_role_id` and `template_version_seen`; the Federation authors templates), AmendRole (set replace, version + 1, optionally taking the template's current version), RetireRole, AssignRole (entity-wide or at a location; a caller scoped to a location assigns there alone), RevokeRole (the row is deleted and audited), SetSodPair and RemoveSodPair (an entity adds or raises its own pairs; federation defaults are never lowered or removed). **Guardrails** (`RoleGuards` over `RoleRules`, the kernel helpers 19A section 3 names but does not declare): nobody grants a permission they do not hold, on a role or by assigning it; FEDERATION-scope permissions only in federation-owned roles; no role, and no person through two roles, holds both halves of a ROLE-mode pair; limits against `limits_schema`; never the last `gov.user.manage` holder; retire only when nobody holds the role; no ROLE raise while someone holds both. **Template drift** (doc 19 DR-4, assumes doc 10 E-04): `GET /v1/security/roles/{roleId}/diff` and the `templateUpdated` marker; nothing is pushed into clones. **Events** `role.changed.v1` (no userId: the kernel empties the whole permission cache), `role.assigned.v1` and `role.revoked.v1` (with `userId`: that user's entries), `sod_pair.changed.v1`; audit types ROLE_CHANGED, ROLE_ASSIGNED, ROLE_REVOKED, SOD_PAIR_CHANGED; 37 message ids in three languages; slice paths under `/v1/security/roles`, `/assignments`, `/sod-pairs`; the web client regenerated. **Migration** `m1security/V0010`: DELETE on `user_role`, `role_permission` and `sod_pair` with `own_delete` policies (named in `SchemaRulesIntegrationTest.DELETE_BY_DESIGN`: 21A makes revoke a delete, 17A says no DELETE anywhere; the guide's procedure is followed for these three tables only), the Federation's `federation_template_*` policies (the fifth class of the policy template) and `security.role_assignment_count` with a `definer_read` policy for app_seed on `user_role` and `app_user`. **Decisions**: RevokeRole is `POST /v1/security/assignments/revoke`, not DELETE (the idempotency hash covers path and body, not the query string); the Federation's own roles may carry FEDERATION-scope permissions (doc 21 section 3.5 "federation-owned roles"; the 21A pseudocode says templates only); the grantor check also at assignment and the ROLE-mode check per person (doc 19 section 3.2); retiring does not raise the version (a retired template would otherwise mark every clone as drifted); the limits reader takes a small subset of JSON Schema (no library in the catalogue). All in `m1party/README.md`. **Found on the way, not fixed here**: `SET row_security = off` in a SECURITY DEFINER function owned by the migrator fails under FORCE ROW LEVEL SECURITY ("query would be affected by row-level security policy"), which is how `security.entity_has_user_manager` (m1security V0003, ActivateEntity) is written; no integration test calls it; and `JdbcUserScopes.loadGrants` selects `external_grant.user_id`, a column that does not exist (it is `grantee_user_id`). **Not here**: the shop-in-charge template and the pos/shop/inv codes of 21A section 3.3 (the catalogue freeze, M1-13, and the owning modules), a notification on template change (a K-10 rule for M9), web screens (M1-12), snapshot change-log producers (M1-10). **Proof**: `BuildingARoleForAShopScenarioIntegrationTest` (flow 6.3 over HTTP with signed tokens: template, clone with a duty added, refusals for a permission not held, a ROLE pair, a location and a user of another entity, assignment at the shop, the resolver gives the duties at that shop and nowhere else, the Federation amends the template, the clone shows the marker and the diff, the administrator adopts it; five events and five audit records, the refusals none); `RevokeStopsThePermissionPostgresIntegrationTest` (the outbox row of `role.revoked.v1` delivered by the kernel's EventConsumerDispatcher to `kernel-permission-cache`: allowed before, still cached after the revoke, refused after delivery); `RoleHandlersPostgresIntegrationTest` (50 cases: every handler's success with its audit record and event, every guard with its message id and nothing committed, RLS rows for the new policies); `RoleGuardrailsPropertyPostgresIntegrationTest` and `RoleRulesPropertyTest` (the two properties of 21A section 9, as randomised JUnit tests with fixed seeds because jqwik is not in the catalogue: 200 random edits and 150 random assignments through the handlers, 1,000 generated cases each on the functions); `RoleRulesTest`.
- **M1-07 users and credentials (25 Sep, branch `feat/m1-07-users-and-credentials`, on top of K-02's second pull request).** The user commands of 21A sections 5 and 6 in `m1party/internal/user`, all `gov.user.manage` with MFA in an entity-wide OWN scope (a shop-scoped session is refused, `m1.user.entity_scope_required`), under `/v1/security/users` in the slice (`listUsers`, `getUser` with the new `gov.user.view`; `createUser`, `updateUser`, `resetCredential`, `deactivateUser`). **CreateUser**: home entity the caller's (ADR-18), kind, language, user name (lower case, unique in the federation through `security.username_taken`, a definer function that answers a boolean only), display name, `succeedsUserId` a deactivated user of the entity (doc 21 DR-5); the row PENDING, then the login through `IdentityProviderClient.createUser`, then the subject on `provider_subject`; `USER_CREATED`, `user.created.v1`. **UpdateUser**: name shown, language, kind among BACK_OFFICE, TILL and BOTH (never to or from EXTERNAL); a user who stops working a till loses the PIN; `USER_UPDATED`, `user.updated.v1`. **ResetCredential**: PASSWORD (the provider's one-time password), SECOND_FACTOR (TOTP reset) or PIN; a PENDING or LOCKED user becomes ACTIVE with a new password or PIN (`USER_ACTIVATED`, `user.activated.v1`, beside `USER_CREDENTIAL_RESET`, `user.credential_reset.v1`). The temporary password goes out through `Notifications.send` when M9 holds an ACTIVE rule under the key `user.temporary_password` whose role audience resolves; otherwise it is answered once (`delivery: RETURNED`, `Cache-Control: no-store`), `@JsonIgnore` on the result so the idempotency store never holds it, and a replay answers without it. **PIN policy** (doc 19 section 2.1 and DR-5; doc 21 section 7): digits only, 4 to 6 as the entity sets (`security.pin.length_min`/`length_max`, narrowed never widened), none of the last three (`security.pin.history_depth`), Argon2id through `PinHasher`, the history rotated in `pin_history`; `security.pin.lockout_attempts` (5) and `lockout_duration` (PT15M) are till-visible items for the till to enforce. The five items are typed in `seed/kernel/config-items.yaml` (module m1party, like `m1.bulk.max_rows`) with defaults in `seed/m1party/config.yaml`. **DeactivateUser**: in scope, not deactivated, a reason, not the last entity-wide holder of `gov.user.manage` (as ActivateEntity counts them), not the entity's responsible officer (doc 21 DR-1); status DEACTIVATED, the PIN cleared, assignments kept, the login disabled and its sessions ended; `USER_DEACTIVATED`, `user.deactivated.v1` with `userId`, which empties the kernel's permission and scope caches. **Row-level security** (`m1security/V0012`): a shop-scoped session reads the users with an assignment at its shop or an entity-wide one, not a sibling shop's; an entity-wide one all its entity's users. **Decisions**: the user events carry `appUserId` as well as `userId`, because the outbox skips `userId` when it looks for the aggregate id; activation happens when the platform issues the first credential, since there is no provider callback; `user.updated.v1` is new. **Two defects repaired on the way.** (1) `KeycloakAdminClient` read the target's home entity through `SystemScope.inScope` inside the caller's transaction, which left the connection in the federation viewer's scope; the handler's audit record was then refused (the reset answered 500 in the provider test with the old code). The read now runs in a transaction of its own. (2) A `SECURITY DEFINER` function with `SET row_security = off` fails on a `FORCE`d table ("query would be affected by row-level security policy"), so M1-03's `entity_has_user_manager` and `user_belongs_to_entity` could never have read a user (their tests stub them); V0012 admits the definer with read policies `TO app_seed` on `app_user` and `user_role` and removes the setting from both. **Not here**: "no open till session" before deactivation (waits for M6's query, as in M1-05); the provider's locale when the language changes (the wrapper has no update call); counting till PIN failures and `account.locked_out.v1` (till sync); the operator snapshot (M1-10). **For the architect**: the idempotency filter hashes the request body with plain SHA-256 and stores the hash, so a four-to-six-digit PIN in a reset request can be recovered from `kernel.idempotency_key.request_hash` by trying every PIN; a keyed hash (HMAC with a server secret) would close it, and it is a kernel change. **Proof**: `UsersAgainstTheProviderIntegrationTest` (the done criterion: through the HTTP API against the provider started with `infra/compose/realm-dev.json`, create, then reset, then deactivate, checked after each step as the realm administrator and by signing in: the one-time password is right and must be changed, then the account is disabled; four audit records and four events), `UsersPostgresIntegrationTest` (every guard with its message id and nothing committed; the audit states and events; the PIN hashed with Argon2id and absent from audit and events; the policy's digits, length, the entity's narrower length and the last three; LOCKED unlocked; the last user manager and the responsible officer; the shop-scoped reads and updates; the definer functions; over HTTP the password answered once, never in the idempotency store, and a PIN of the wrong shape refused before the handler without being echoed), `PinPolicyTest`, `TemporaryPasswordDeliveryTest`.
- **K-01 close-out: the policy template and the RLS matrix (24 Sep, branch `feat/k01-rls-policy-template-and-matrix`).** K-01 (#66) merged without the two deliverables 19A section 1 names beside the scope filter: the PARTY policy template and "the RLS matrix of 17A section 12 extended: OWN, PARTY, FEDERATION_VIEW, EXTERNAL grants, NONE; a transaction that forgets the customizer sees zero rows". Both exist now: `db/migration/RLS_POLICY_TEMPLATE.md` is the five-class template as SQL to copy, with the three cases it does not cover and what does (reference data through `app_seed`, the Federation through a guard, a user's own rows through `app.user_id`), and `RlsMatrixIntegrationTest` applies it to a throwaway table and proves nine rows of the matrix as the application user. **The matrix found two holes in the template as 17A section 6.3 and 19A section 1 write it**, raised as `CR-17A-3`: `own_read` and `own_write` test the entity and not the class, so a FEDERATION_VIEW or EXTERNAL caller, whose scope entity the kernel also sets, read and wrote that entity's rows through them, against doc 18 section 3.7 (those classes write nothing; EXTERNAL reads its grant only); and 19A's PARTY policy has no location clause, so a shop-scoped user read every document of the whole entity and lost `own_read`'s location clause as well. The template in the repository carries the corrected text; hello's two policies are altered here (`hello/V0002`); the other 38 `own_*` policies on `main` (`kernel.audit_event` 4, `party` 15, `security` 17, `pricing` 2) are their owners' to alter, with the exact statement in the change request, and the two partition functions of K-04 and K-05 with them. One correction to a review: the K-04 review of 23 September asked for the class test to be dropped from `own_write` on `kernel.audit_event`; the matrix shows doc 18 needs it, and the review was wrong on that point.
- **M1-06 devices (25 Sep, branch `feat/m1-06-devices`).** The device aggregate of 21A section 3.1 and the five device commands of section 6, done when scenario 6.6 of doc 21 (replacing a faulty till) runs. **Schema** (`m1party/V0009`): `party.device` gets `location_id` (NOT NULL; no handler wrote a device before), the template's location line on `own_read`, `own_write` and `own_update`, `ext_view`, and a check that only an ACTIVE or SUSPENDED device holds a position. **Commands** (`internal/device`, `POST /v1/party/devices` and `/{deviceId}/assign|suspend|reinstate|retire`, `GET` list and one): **EnrolDevice** (`sys.device.enrol`, MFA: the caller's own location, a known kind, a staging reference, a well-formed release number, a serial unique across the federation, the unique key translated for a serial another entity holds; ENROLLED; `DEVICE_ENROLLED`, `device.enrolled.v1`); **AssignDeviceToPosition** (`sys.device.enrol`, MFA: ENROLLED or ACTIVE; a till terminal; the position visible, ACTIVE and at the device's location; free, where a position held by a SUSPENDED device is free for its replacement and held by an ACTIVE one is occupied; the release at or above `m1.device.version_floor`; a reason; every device that gives a lane up drained (`kernel.api.SyncStatus`) or its loss recorded; then the old holder lets go, the device is ACTIVE at the position, and the counters of the position, and of the shop when it is the primary till, move through `NumberingService.holderChange`, which audits and publishes `series.holder_changed.v1`; `DEVICE_ASSIGNED` and `device.assigned.v1` for a free position, `DEVICE_POSITION_CHANGED` and `device.position_changed.v1` for a replacement or a move, plus a `DEVICE_OUTBOX_LOSS_RECORDED` ALERT when the loss was recorded); **SuspendDevice** (`sys.device.suspend`, MFA: ACTIVE; a reason LOST, STOLEN or FAULTY; SUSPENDED, keeping its position; `DEVICE_SUSPENDED`, `device.suspended.v1` and `device.revoked.v1`); **ReinstateDevice** (SUSPENDED; a reason; ACTIVE, back in its own lane or without one if it was replaced; `device.reinstated.v1`, which lifts the revoke); **RetireDevice** (not RETIRED; unassigned; a reason; `device.retired.v1` and `device.revoked.v1`). `DeviceQueries` lists and gets devices with the position, its number and the primary-till flag. **The revoke instruction** the sync gateway relays to the till (doc 32 section 9) is `device.revoked.v1`, with the serial and the cause, so K-08 acts without reading M1's tables. **Decisions, said out loud for the architect:** (1) `kernel.api.SyncStatus` is a new kernel contract (21A names "kernel SyncStatus.drained"); until K-08 its only implementation, `NoGatewaySyncStatus`, answers "not drained" for every device, the safe side, so every replacement needs the loss recorded until then. (2) A suspended device keeps its position (21A: "keeps position for return"), which is what makes "position free" and "the previous device" of the 21A pseudocode both true: the previous holder is read from the database, not taken from the request. (3) MFA on every enrol and assign, not only on re-assign as doc 21 section 4.5 has it, because the catalogue marks `sys.device.enrol` `mfa: true` (21A section 3, the seed excerpt) and the interceptor checks the permission. (4) Enrolment is by the owning entity in its own scope; the Federation staging table of doc 31 is in no DDL, so the staging reference is carried in the command and kept in the audit record. (5) A WORKSTATION or DRIVER_MOBILE does not take a till position, and a device is assigned only at its own location. (6) The two 21A permission codes are added beside the existing `sys.device.manage` (now unused; for the catalogue freeze, `CR-21A-1`). **Not here:** the device credential and its authentication (K-02), the enrol endpoint and the gateway's revoke, heartbeat and sequence reset (K-08), the asset register screen (M1-12), and the till position handlers themselves (M1-05, #102). **To reconcile when #102 merges:** its test fixture `insertDevice` must give `location_id`; `M1SeedLoaderTest` counts 22 permissions; the device package may then use `TillPosition` and `SeriesHooks` instead of `DevicePlaces`, and a later migration can add the foreign key `(current_till_position_id, location_id)` on the key #102 creates. **Proof** (`DevicesPostgresIntegrationTest`, as the application user, and `AppVersionTest`): scenario 6.6 end to end (suspend and revoke; the drained check refuses and commits nothing; the loss recorded, the three series move to the replacement with their numbers untouched and the kernel's `series.holder_changed.v1` from the old device to the new; the old device retired and revoked); a drained device needs no loss; a first assignment moves the position's and the primary till's counters, another position only its own; a move to another position needs the device drained; reinstatement into its own lane and without one after a replacement; every guard of the five commands as a failing case with its message id and nothing audited or published; a shop-scoped caller lists, reads and manages only its shop's devices, the Federation view reads all and writes nothing, and the policies alone refuse an insert or update at a sibling shop; over HTTP, enrol, a malformed release (400), assign, a second assign (422 with the message id), a suspend reason outside the enumeration (400), suspend, list.
- **K-08 sync gateway, first part (25 Sep, branch `feat/k-08-sync-gateway-1`, started on `feat/k-02-identity-2`, merged with main after M1-04 to M1-09).** Doc 32 as 19A section 8 restates it, the half the conformance cases of channel 1 need; the snapshot builder, the rate limiter and the till simulator are the second part. **Slice** `openapi/sync.yaml` (served by the kernel: `build.gradle.kts` generates it into `kernel.sync.web.generated`, so `sync` is not a module): enrolment codes, enrolment, batches, heartbeat, the change log, the snapshot (501 until part 2), attachment presign; the envelope (`TillEvent`), the acknowledgement, the instructions, every refusal as a problem document. **Schema** (`kernel/V0080`, lane D): `device_sync_cursor` (the only sync state: last applied, last batch and its acknowledgement, the batch in flight, gap count, resend request), `sync_event` (one row per sequence a device sent, applied or quarantined; an event id applied once), `sync_quarantine` (raw, with its reason), `device_heartbeat`, `device_enrolment_code` (hash only), `location_snapshot_version` and `change_log` (written only through the `SECURITY DEFINER` function `kernel.change_log_append`, as `app_seed`). **Device authentication** (`DeviceAuth`): a token with `cls = DEVICE` and `dev` is the device, not a user; its scope is OWN at its shop from M1's `party.device` (read as the federation viewer, cached 60 s, emptied by M1's `device.*` events through `DeviceCacheInvalidator`); SUSPENDED and RETIRED are 403 with a revoke instruction signed Ed25519 (`TillSigner`, the key from `coop-erp.sync.signing.*`, an instance key when unset); ENROLLED or unassigned is 403 `sync.device_not_active`. The scope filter refuses a user token on a device operation (`sync.device_token_required`) and a device token anywhere else (`sync.device_token_not_allowed`). **Enrolment** (`EnrolmentService`): an administrator (`sys.device.manage`, with the catalogue's MFA flag, under the enforcement switch) issues a sixteen-character code for a device of its entity; the till presents it once, without a token, with its hardware serial, and receives its credential (`DeviceCredentials` in `kernel.api`, implemented by `KeycloakDeviceCredentials`: one confidential client per device whose client-credentials tokens carry `dev` and `cls = DEVICE`), the next sequence, the series of its position (and of its location when it is the primary till), the snapshot pointer and the signing key; `DEVICE_SYNC_ENROLLED` and `device.sync_enrolled.v1` for M1. **Batch ingestor** (`BatchIngestor`, `IngestTransactions`, `EventApplier`): shape and DR-1 limits (413), the application floor (426), the cursor claimed with `FOR UPDATE NOWAIT` and an in-flight marker (409 `sync.batch_in_flight`), the same batch again answered with its stored acknowledgement, a gap 409 with `expected_seq` (repeated: `SYNC_ANOMALY`), a replay answered from state, the new events in chunks of `sync.ingest.chunk_size`, each accepted into the outbox with the device as source and its sequence as `source_seq` (`DeviceEventWriter`; the module consumers get them from there) or quarantined (SCHEMA, DUPLICATE_ID, HASH over the bundle's document and lines by the `ContentHash` rule, TOO_LARGE, FORBIDDEN_FIELD) with `SYNC_EVENT_QUARANTINED` (ALERT) and `sync.anomaly.v1`; `sync.batch_received.v1` per batch; gzip bodies inflated by `GzipRequestFilter`. **Heartbeat** (`HeartbeatService`): the fleet-health fields of doc 32 section 6 into the kernel's table, the clock offset, `SYNC_CLOCK_DRIFT` (REVIEW) once per drift, `RESEND_FROM` with `SYNC_CURSOR_BEHIND` (REVIEW) when the device holds more acknowledged than central (a restore), `FLOOR_NOTICE`, the urgent-change flag; `SyncStatus` (`kernel.api`, which M1-06 declared with `drained(deviceId)` and a stub that answered false) is now `JdbcSyncStatus`: `drained` keeps M1-06's signature and reads in the caller's transaction under its scope (drained when the last heartbeat reported nothing pending, no batch is in flight and central holds all the device holds acknowledged; unknown is false), and `state(deviceId, ctx)` is added for M1's device list. The device directory takes an unassigned device's location from M1-06's `party.device.location_id`, and the device cache also empties on `device.revoked.v1`. **Change log**: `ChangeLog.append(targets, changes, applyFrom, urgent, ctx)` in the producer's transaction, one version per publication per shop; `GET /v1/sync/locations/{id}/changes?since=N` pages on version boundaries and says when a full snapshot is required (no version, older than `sync.change_log.retention`, or ahead of central). Ten `sync.*` configuration items (DR-1, DR-4, DR-5 and the rest), six audit types, eighteen messages in three languages. **Decisions** (the pull request lists them in full): paths `/v1/sync/...` where doc 32 writes `/sync/v1/...` (the slice rule); snake_case on the wire as doc 32 names the fields; the device credential is a provider client per device, not a kernel-signed token (the service account gains `manage-clients`; the provider caps the requested 24-hour token at the realm's session maximum, ten hours in the dev realm); ingestion runs in the request, where the web role is, until doc 35 routes an ingest role; the ledger `sync_event` instead of `event_inbox('sync')`, which has no sequence; FLAGGED is the consumers' to raise, the acknowledgement says APPLIED, DUPLICATE or QUARANTINED; `/changes` is an operation doc 32 does not name; a suspended device keeps its credential so that it can be told, a retired one loses it. **Not here** (part 2): the snapshot builder and contributors, the full snapshot files and their job, the rate limiter, the till simulator, the bundle handler into the document base and the kernel's consumer of the till's `audit.*`, the sequence-reset recovery, the change-log purge, and the conformance cases Two instances, Snapshot atomicity, apply_from, Counter transfer, Burst, Business-rule breach and Power loss on the till. **Proof**: the conformance cases of doc 32 section 11 this part covers, one class per group against the running application with device tokens: `BatchUploadConformanceIntegrationTest` (Lost acknowledgement, Gap, replay, overlap, a duplicate id quarantined, repeated gaps, the batch shape, gzip, not enrolled), `QuarantineConformanceIntegrationTest` (Malformed event, bundle hash, personal data), `CrashAndConcurrencyConformanceIntegrationTest` (Crash mid-batch at central, Concurrent batches, a dead instance's claim), `HeartbeatConformanceIntegrationTest` (Central point-in-time restore, the health fields, drift, urgent change, `SyncStatus`); and `DeviceAuthConformanceIntegrationTest`, `EnrolmentIntegrationTest`, `ChangeLogIntegrationTest`, `KeycloakDeviceCredentialsIntegrationTest` (the provider of the local stack), `JwtClaimsMapperTest`, `ScopeFilterPrincipalTest`, `BundleHashTest`, `SyncUnitTest`.
- **Identity and permissions, the review fixes (25 Sep, branch `fix/identity-review`).** The code review of the twenty-one pull requests of 24 and 25 September found four access-control defects in K-02 and K-03b and eight lesser ones; this fixes them together, in the kernel's security package. **Scopes are the assignments and nothing else** (`JwtClaimsMapper`): the home entity is where a user belongs, not where they act, so a cashier assigned at one shop no longer reads the whole society entity-wide; a read-only class keeps its home entity as the nominal scope; the test provider carries a `scopes` claim as 19A's provider mapper would. **An entity-wide holder may act at any location of the entity** (`ScopeFilter.holds`, doc 19 section 3.1). **A login the platform creates carries `ent` and `cls`** (`KeycloakAdminClient.createUser`), so a new user's tokens have a class. **User management is entity-wide** in the provider client: a location-scoped caller is refused, so a shop manager cannot reset the entity's administrators. **`resetTotp` keeps the pending required actions.** **Tokens must be issued to a client of the platform** (`AcceptedClientsValidator`, `coop-erp.security.oidc.accepted-clients`, tills by prefix). **The second factor from standard claims**: `mfa_at`, else `auth_time` when `acr`/`amr` names a second factor, or on a development stack where a fresh password sign-in counts (`coop-erp.security.mfa.password-reauth-counts`, compose only). **One freshness rule** (`StepUp`, the register's `security.mfa.freshness`, twelve hours by default) for the interceptor, the register and the enrolment store. **The permission check runs before the idempotency branch**, for every caller that names a user. **Caches empty on the changing instance after commit** (`PublishedEventListener` from `OutboxWriter`; a load that raced the commit can refill an old entry, so the promise is within a minute, corrected 26 Sep), the grant events empty the grant cache, the expiry is a minute, and the configuration register's cache empties after commit rather than before. **A ROLE pair refuses the same person** (`JdbcSod`). **Not here**: the per-instance fan-out queue on every profile, enforcement on by default, the web shell's replay after step-up. **Proof**: `JwtClaimsMapperTest`, `StepUpTest`, `TokenAuthenticationPostgresIntegrationTest` (another client refused, an entity-wide holder at a location), `KeycloakAdminClientIntegrationTest` (ent/cls, a shop-scoped caller refused, UPDATE_PASSWORD kept), `PermissionResolverPostgresIntegrationTest` (a ROLE pair), and the hello, M1, M3, sync, config and role suites.
- **Identity and permissions, the review fixes (25 Sep, branch `fix/identity-review`).** The code review of the twenty-one pull requests of 24 and 25 September found four access-control defects in K-02 and K-03b and eight lesser ones; this fixes them together, in the kernel's security package. **Scopes are the assignments and nothing else** (`JwtClaimsMapper`): the home entity is where a user belongs, not where they act, so a cashier assigned at one shop no longer reads the whole society entity-wide; a read-only class keeps its home entity as the nominal scope; the test provider carries a `scopes` claim as 19A's provider mapper would. **An entity-wide holder may act at any location of the entity** (`ScopeFilter.holds`, doc 19 section 3.1). **A login the platform creates carries `ent` and `cls`** (`KeycloakAdminClient.createUser`), so a new user's tokens have a class. **User management is entity-wide** in the provider client: a location-scoped caller is refused, so a shop manager cannot reset the entity's administrators. **`resetTotp` keeps the pending required actions.** **Tokens must be issued to a client of the platform** (`AcceptedClientsValidator`, `coop-erp.security.oidc.accepted-clients`, tills by prefix). **The second factor from standard claims**: `mfa_at`, else `auth_time` when `acr`/`amr` names a second factor, or on a development stack where a fresh password sign-in counts (`coop-erp.security.mfa.password-reauth-counts`, compose only). **One freshness rule** (`StepUp`, the register's `security.mfa.freshness`, twelve hours by default) for the interceptor, the register and the enrolment store. **The permission check runs before the idempotency branch**, for every caller that names a user. **Caches empty on the changing instance after commit** (`PublishedEventListener` from `OutboxWriter`), the grant events empty the grant cache, the expiry is a minute, and the configuration register's cache empties after commit rather than before. **A ROLE pair refuses the same person** (`JdbcSod`). **Not here**: the per-instance fan-out queue on every profile, enforcement on by default, the web shell's replay after step-up. **Proof**: `JwtClaimsMapperTest`, `StepUpTest`, `TokenAuthenticationPostgresIntegrationTest` (another client refused, an entity-wide holder at a location), `KeycloakAdminClientIntegrationTest` (ent/cls, a shop-scoped caller refused, UPDATE_PASSWORD kept), `PermissionResolverPostgresIntegrationTest` (a ROLE pair), and the hello, M1, M3, sync, config and role suites.
- **M2-02 SKU aggregate and search (25 Sep, branch `feat/m2-02-sku`, pull request #115, by Pavani).** The catalogue item of 22A sections 3 and 6 in `m2catalogue/internal/sku`, the first operations of the M2 slice under `/v1/catalogue/skus` (`createSku`, `listSkus` with `q` for search, `getSku`, `updateSku`, `activateSku` with `target` LOCAL or SHARED, `deactivateSku`, `reactivateSku`), each mutation with its `Idempotency-Key` and `x-permission`. **The aggregate** `Sku`: a DRAFT with a code from `SkuCodeGenerator` (`SKU-` and eight letters of a 32-letter alphabet), the owner the caller's entity; DRAFT to LOCAL or SHARED, LOCAL or SHARED to INACTIVE with the prior status kept, and back; a SHARED SKU keeps its three names (B-I1); sold by weight needs KG, expiry tracking needs batch tracking; attributes at most 4 KB. **The guards** (`SkuRules`, `SkuGuards`): an entity-wide OWN scope, the English name, a unit and a tax category that exist, a reason for deactivation and reactivation, the owner for every change; the unit and the tracking flags cannot change once lots exist, behind an `InventoryLotQuery` seam that answers "no lots" until M5 supplies it (`NoInventoryLotQuery`). Audits `SKU_CREATED`, `SKU_ACTIVATED`, `SKU_UPDATED`, `SKU_DEACTIVATED`, `SKU_REACTIVATED` with before and after, and the matching `sku.*.v1` events. **The reads** (`CatalogueQueries`): list and search under row-level security as it stands (own rows, SHARED rows for everyone, all rows for the Federation view), ordered by the display language's collation, attributes only for the owner. `cat.sku.view` joined the three role templates. **Deviations**: no duplicate scoring and no `duplicate.suspected.v1` yet (22A section 6 asks for them on create and update; they come with the `duplicate_suspect` table); the list pages by offset where 22A section 4 asks for a cursor. **Proof**: `SkuHandlersPostgresIntegrationTest`, `CatalogueQueriesPostgresIntegrationTest`, `CatalogueHttpPostgresIntegrationTest` (the lifecycle through the generated contract), `SkuTest`, `M1SeedLoaderTest`. The review of 26 September found the shared path open to any entity and a few lesser points, fixed in the next entry.
- **M2-02 review fixes (26 Sep, branch `fix/m2-02-review`).** The findings of the review of pull request #115, in `m2catalogue`. **The shared path is the Federation's** (`FederationCaller`): creating a shared SKU, activating one and editing one require the caller's entity to be the Federation, the entity `coop-erp.system.entity-id` names (M2 may not read M1's entity table; M2SeedLoader and the kernel's federation-wide configuration use the same setting), refused with `m2.sku.federation_only`; the permission alone was not enough while enforcement is off. **The router follows the SKU** (`SkuCommandRouter`): an update goes to the shared path when the SKU is SHARED and to the local path otherwise, never by the caller's permissions, and the local handler refuses a SHARED SKU; create always makes a DRAFT, so the byte-identical `CreateSharedSkuHandler` is gone and create is `cat.sku.create_local` alone. **Code clashes**: under row-level security the handler cannot see other entities' codes, so a clash surfaced as a 500; the router now runs the create again with a fresh code when the insert meets the unique index on `sku_code` (at most five times, then `m2.sku.code_generation_failed`). **Search** (22A section 7): the name columns bare so their trigram indexes apply, the code by prefix (`m2catalogue/V0002` adds a `varchar_pattern_ops` index for it), `%`, `_` and `\` in the text escaped, the closest match first after the collation order; the offset is capped at 10,000 (also `maximum` in the slice). **Shared activation** audits `SKU_SHARED` and publishes `sku.shared.v1` (`SkuShared`); the local path keeps `SKU_ACTIVATED` and `sku.activated.v1`. **Reads** check `cat.sku.view` in the controller for the OWN class when permissions are enforced (no command interceptor runs for a read); a missing SKU is the problem `m2.sku.not_found`, now 404 in `ProblemResponses`, and the slice says so; the create's "not visible after create" is that problem too instead of an `IllegalStateException`. **The lots guard** has its own message, `m2.sku.has_lots`. **Role templates**: the Entity Administrator gains `cat.sku.create_local` and `cat.sku.deactivate`; a new OWN template, Federation Catalogue Steward, holds `cat.sku.view`, `create_local`, `create`, `promote` and `deactivate` (four templates now). **Deviations**: the offset cap is a constant, not a configuration item, until the list moves to the cursor of 22A section 4; duplicate scoring stays deferred as above. `make reset` is not needed (V0002 is a plain new migration). **Proof**: `SkuHandlersPostgresIntegrationTest` (a society refused the shared path; the router by status; the Federation refused another entity's LOCAL SKU; a forced code clash retried, and one that keeps clashing ending in the problem; the lots guard with a stub `InventoryLotQuery`; `SKU_SHARED` and `SkuShared`), `CatalogueQueriesPostgresIntegrationTest` (`%`, `_`, `\` are letters; the code by prefix only; similarity after the collation; the offset cap), `CatalogueHttpPostgresIntegrationTest` (a replayed create answers the first SKU and creates nothing; the 404 problem), `CatalogueControllerViewPermissionTest`, `M1SeedLoaderTest`.
- **The RLS matrix runs against every real table, with update and delete (26 Sep, branch `fix/rls-matrix-review`).** The review of K-01's close-out (#89) found that the matrix proved a copy of the template on a throwaway table in `hello`, not the template file and not a single real table, and had no UPDATE or DELETE. `RlsMatrixIntegrationTest` now reads the SQL block of `RLS_POLICY_TEMPLATE.md` into a table in a schema of its own, then discovers every table with an `owner_entity_id` column from `pg_catalog` and runs the same matrix against its real policies: SELECT, INSERT, UPDATE and DELETE for OWN (entity-wide and at a location), PARTY (also at a location), FEDERATION_VIEW, EXTERNAL_TIMEBOXED (three grants), NONE with an entity and a grant naming it, and a transaction with no scope, plus "OWN cannot move a row to another owner"; each table in one rolled-back superuser transaction with the checks run as `coop_app` in savepoints. FORCE on every owned table and partition, and the class test in each clause of every `own_*` policy, are asserted. Where a table departs, the test lists it with the reason (reference reads in the catalogue; no `ext_view` on `security.app_user` and `external_grant`, on purpose). **The template** gains `own_update` and `own_delete` (class test in USING and WITH CHECK, where the table grants the operation), where the EXTERNAL time box is enforced (the grants the scope customizer resolves; `ExternalGrantsIntegrationTest` proves expiry), and `party_read` with the location line on the owner's side only, noted in `CR-17A-3`. **Found by the matrix, left to the owning modules** (merged migrations are not edited; each is an explicit expectation with a TODO): `kernel.document.party_read` still has the location line on both sides; `hello.greeting`, `pricing.price_list` and `party.entity` have no `ext_view`.
- **Faster pull request pipeline (26 Sep, branch `ci/faster-pull-requests`).** From the CI timing analysis of 26 Sep. On a pull request `package` no longer waits for build-verify-test (on main `package-main` runs the same steps, a YAML anchor, after a green build-verify-test; the pull request check keeps the name "Package") and skips `make image`, which stack-smoke already builds and runs; the old build-verify-test is two jobs, `build-verify` (compile, generated files, unit tests, web) and `integration-test`, each about half of the old one, so a pull request answers in the time of the longer half; integration tests run in the forks that `COOP_ERP_INTEGRATION_FORKS` names (two on a laptop, one PostgreSQL container per fork, no shared ports or state; one in the pipeline, where two forks on the two-core runner were slower than one); `make test-scaffold` runs the copy's own tests, the architecture and slice rules and the schema, own-policy and runtime-role integration tests instead of the whole suite, which the nightly run now does on the copy (`SCAFFOLD_TESTS`); `backend/gradle.properties` turns on parallel builds, the build cache and a 2 GB heap; `check-generated` in the pipeline reuses the up-to-date `:app:test` (`ARCH_TESTS`); pnpm is set up before setup-node, which caches the pnpm store; the till job runs its steps only when `till/**`, `backend/shared-engine/**` or ci.yml changed and reports success otherwise. **Deviations:** none of a guide; the scaffolder proof on a pull request is narrower than S0-13 states, the whole proof moves to the nightly run.
- **Event backbone, the review fixes (26 Sep, branch `fix/event-backbone-review`).** The review of #86, #87 and #88 found the payload word list half applied and poison messages lost. **Payload**: `OutboxWriter` checks every field of every event, nested objects and arrays included, with the whole-word rule `DeviceEventWriter` already used, so `customerName`, `email`, `mobile`, `address` and `token` are refused; an identifier or a code (`addressId`, `cityCode`) passes unless its head is a secret (`pinCode`), and so do the catalogue display names doc 18 permits (`nameEn`, `nameSi`, `nameTa`, `uomName`, `productName`). The after-commit listeners run from one synchronisation per transaction, and a failing listener is logged, not thrown. **Dead letters**: consumer queues are declared with `x-dead-letter-exchange ""`, `x-dead-letter-routing-key domain.dlq` and `x-delivery-limit 10`; a `PoisonMessageException` (a consumer/type nobody registered, an envelope without owner or correlation, an envelope that cannot be read) or an unreadable body goes to `domain.dlq` with its reason at once; a handler retry, and any other failure, is requeued in place after a growing backoff, so per-source order holds and the delivery limit ends what never passes. RabbitMQ keeps an existing queue's arguments, so a compose stack needs `make reset`. **Relay**: a relay takes the first unpublished source whose advisory lock it gets, and marks by `(received_at, event_id)`; `Replayer` pages by `source_seq`. **Inbox** (`kernel/V0032`, lane B): a claim carries the scope entity and `app_rw` sees and writes only its scope's claims (`own_*` of the template, no location line). **Idempotency**: a retention window under 24 hours is refused at start. **Deviations**: testsupport has no RabbitMQ container, so the broker-level poison and order test is deferred and the runtime is proved with a mocked channel; the partition job still runs on every role, because a web-only instance needs today's partition; inbox retention and the idempotency of non-HTTP callers (K-08 and K-12 pass their own key) are deferred. **Proof**: `OutboxWriterForbiddenFieldsTest`, `OutboxPayloadValidationPostgresIntegrationTest`, `RabbitConsumerRuntimeTest`, `EventConsumerFrameworkPostgresIntegrationTest`, `OutboxRelayLockPostgresIntegrationTest`, `JdbcIdempotencyStoreWindowTest`, and the event, idempotency, notification and policy suites.
- **Documents and numbering, the review fixes (26 Sep, branch `fix/documents-numbering-review`).** The wave-1 and deep reviews of K-07 (#93): every way an issued document changed behind the protocol is closed. **Schema** (`kernel/V0055`, lane C; V0050 untouched): a BEFORE INSERT trigger on `document_line` and the `document_write` policy (`kernel.document_unissued`) refuse a line under an issued header (`document.immutable`); a partial unique index holds REVERSES at most once per original; `own_read` and `own_update` on `numbering_series` and `document` admit `location_id IS NULL`, so a shop-scoped session finds the ENTITY series and the entity's location-less documents (24B: ORD and DISC are ENTITY series; the matrix test lists both tables as a departure with the reason); `party_read` carries the location line on the owner's side only, as the template says (the matrix expectation with the TODO is gone); `numbering_gaps()` measures a series against the larger of its counter and its highest number, so a series filled by ingestion is checked too. **Issuance** (`IssuanceProtocol`): the stored header is read `FOR UPDATE` and the stored lines are what is issued, the caller's lines stored only when none are; the rows go in the order the database allows (header as a draft, lines, then the one update that issues it, through the package-private `writeIssued`); central refuses an OFFLINE draft, a series a device holds and a TILL_POSITION series with `document.series_device_held` (19A section 7, doc 18 section 5.5). **Repository** (`JdbcDocumentBaseRepository`): `save` refuses an issued document outright and a record carrying a number, an issuance time or a hash (`document.issued_fields_reserved`); `addStateTransition(transition, ctx)` is the one path for a status change, compare-and-set on the status the caller saw (`document.status_conflict`), with the history row and `DOCUMENT_STATUS_CHANGED` audited; `findByIdForUpdate` and `lockForLinking` (a transaction advisory lock keyed by the original, since a row lock on a counterparty's document is not the caller's to take) join the interface. **Links** (`DocumentLinksImpl`) lock the original before summing, so two settlements see each other; a second REVERSES the index refuses maps to `document.link.reversed_already`. **Hash** (`ContentHash`): every decimal is set to its column scale before hashing, so a hash recomputed from the stored rows is the stored hash. **Numbering** (`JdbcNumberingService.registerSeries`): `ON CONFLICT DO NOTHING` then re-select, so two registrations at once give one series. **For the architect**: whether a shop-scoped session should see the entity's ENTITY series and location-less documents at all (the wave-1 finding asked for it; implemented, listed in the pull request). **Not here**: the (source, source_seq) density (K-08 part 2 writes those columns; a note in `numbering_gaps()` and `GapCheck`); the share lock on the business date in issuance (K-13, its own fix); a from-side trigger on `document_link` (below, Deviations). **Proof** (`DocumentBasePostgresIntegrationTest`, thirty cases): no line joins an issued document (policy and trigger, superuser included); a state transition is compare-and-set and audited, a stale one refused with nothing committed, `save` refuses a status change and a hand-numbered draft; a stored draft is issued from its stored header and lines; the hash covers the stored scale (33.33333 stored as 33.3333); central refuses a device-held series, a TILL_POSITION series and an OFFLINE draft, and no number is taken; a shop-scoped session issues from the ENTITY series; a counterparty at a location reads the document and its lines; two settlements of one original see each other (the second waits and is refused `exceeds_balance`); a second REVERSES straight at the repository is refused by the index; four concurrent registrations give one series and one audit record; the gap check measures a series whose counter is behind its documents. `RlsMatrixIntegrationTest` with the two departures and the `party_read` expectation gone; `AttachmentsPostgresIntegrationTest`, `SchemaRulesIntegrationTest` and the unit suite.
- **Web shell, the review fixes (26 Sep, branch `fix/web-shell-review`).** The seven web findings of the review of 26 September (M1-12 part 1). **The step-up replays the command** (doc 19 section 2.2; `shell/api/pendingCommand.ts`, `shell/auth/StepUpReplay.tsx`): a mutation refused with `mfa.required` is kept as plain data (method, address, Idempotency-Key, JSON body; never the token, never a file) in the state of the login request, and after the sign-in the frame of every page takes it again once with the fresh token and the same key, says what became of it, and makes every page re-read; the middleware starts one step-up per page life however many requests were refused at once. The sign-in keeps `prompt=login` (a fresh password sign-in counts on the compose stack, #116) and asks for the second factor with `acr_values` where a realm has one (`VITE_OIDC_STEP_UP_ACR_VALUES`, empty by default). The client sends `X-Scope-Location` when the active scope names a location (the scope context still names none until the switcher exists). **Server-side search of the register** (21A section 8): `listEntities` takes `q` (a prefix of the code, a part of any of the three names, `%`, `_` and `\` escaped as M2's search does; a trading partner searches the directory by name), the register page sends the search box to the server and says "no societies match" only when no page remains. **ReasonCapture starts empty** (`shell.reason.choose`), so the reason in the audit record is the clerk's. **A calendar date is formatted as a day** (`useFormatDate`, UTC, no time): the governance signature no longer shows 5:30 AM. **A read-only class is shown no command** (`shell/auth/permissions.ts`: FEDERATION_VIEW, PARTY and EXTERNAL_TIMEBOXED hold reads only, the rule of 19A section 3's resolver), the two register forms are behind `gov.entity.register`, and the e2e test asserts fed-admin sees the register and no form. **A fact with no value** in `DocumentHeader` is an `aria-hidden` dash plus a visually hidden `shell.fact.empty`, as `MoneyDisplay` does. **Deviations**: the temporary role map stays, below. **Proof**: `client.test.ts` (the command handed to the step-up, one step-up for two refusals, `X-Scope-Location`), `pendingCommand.test.ts`, `StepUpReplay.test.tsx`, `permissions.test.ts`, `components.test.tsx`, `formats.test.tsx`, `router.test.tsx`; `PartyQueriesImplTest` and `EntitySearchPostgresIntegrationTest` (the three languages, the wildcards as letters); the Playwright run through the pipeline's stack-smoke job.
- **Roles, users and grants, the review fixes (26 Sep, branch `fix/m1-roles-users-grants-review`).** The wave-1 findings on M1-07, M1-08 and M1-09 and the deep review of M1-09. **Holdings are counted entity-wide** (`m1security/V0013`, `security.user_permissions_at`, a narrow SECURITY DEFINER function like `role_assignment_count`): AssignRole's per-person ROLE-mode check no longer reads the user's assignments under a shop-scoped caller's own policy, which hid the half held at another shop. **The guards that count and then write run under a per-entity lock** (`m1party.internal.security.EntityLock`, `pg_advisory_xact_lock` on the entity id, taken before the facts are read): AssignRole, RevokeRole, SetSodPair, AmendRole and DeactivateUser; two revocations of the last two user managers at once now leave one (proved on two threads). **A template held outside the Federation** keeps `gov.user.manage` and takes no FEDERATION-scope code (`security.template_assignments_outside_federation`, `m1.role.template_held_outside_federation`): AmendRole on a template ran no guard on the people who hold it. **A DEACTIVATED user holds nothing** (`SecurityRecords.assignmentsAt`, `usersHoldingBoth`), so a departed holder of both halves no longer blocks raising a pair to ROLE; **only an ACTIVE user is a user manager** (`UserFacts`, `SecurityRecords.userManagerHoldings`): the resolver grants a PENDING or LOCKED user nothing, so one could not be the other manager who lets the last one go. **No limits is refused where the schema requires one** (`RoleRules.limitsProblem`). **`listRoles` loads every role's permissions in one query.** **A temporary password is NOTIFIED only when somebody was reached**: `kernel.api.Notifications.send` now answers a `Delivery` (the id and SENT, QUEUED, SUPPRESSED or FAILED; a repeat inside the hour counts as suppressed), and `TemporaryPasswordDelivery` falls back to RETURNED unless one recipient was really SENT to, so a suppressed or queued send never drops a password the provider already replaced. **A kind that loses the back office closes the login** (`UpdateUserHandler`: BACK_OFFICE or BOTH to TILL disables the login and ends the sessions, as DeactivateUser does; back to the back office enables it again through the new `IdentityProviderClient.enableUser`); 21A section 6 names no guard on the kind, so the change is allowed and the login follows it rather than refused. **`username_taken` compares the column as stored** (lower case, `CreateUserHandler`), so the unique index serves it (V0013). **The user, device and grant reads check their x-permission in the controller** when enforcement is on (`gov.user.view`, `sys.device.view`, `gov.external.grant`), the pattern of #120's `CatalogueController`. **Grants (deep M1-09):** `JdbcUserScopes.grantsOf` joins the grantee ACTIVE (deactivation ends a grant; the row stays in the register for the Federation to revoke) and keeps a cache entry no longer than the earliest `valid_until` in it; a token's `grants` claim is intersected with the records and never widens them, and only an EXTERNAL_TIMEBOXED caller has any; `ExternalGrantsIntegrationTest` asserts on `grantsOf`, the path a request takes, for the window, a revocation while cached, an ended grant and a deactivated grantee, and the never-called M1 query `activeGrantedEntities` is deleted with its `GranteeGrants` bean (the `grantee_read` policy of V0009 stays, unread: a merged migration is never edited); `ExternalGrantExpiryJob` logs `not_active` and `not_due` at debug; the M1-09 entry above and the module README no longer say the window is checked "at every resolution" by that query or that `party` has no `ext_view`. **Raised, not changed:** `CR-21A-2`, whether M1 may delete `role_permission` and `sod_pair` rows (V0010 granted it; 21A names `user_role` alone). **For the architect:** enforcing a GET's x-permission in the kernel on the generated interfaces rather than per controller; how an EXTERNAL principal obtains read permissions at all (the resolver grants none outside OWN, `AssignRole` refuses EXTERNAL roles, so flow 6.5 cannot yet run end to end over HTTP). **Proof:** `RoleHandlersPostgresIntegrationTest` (the other-shop half refused for a shop-scoped grantor; the template refusals and the free change for a template held by the Federation or a departed user alone; the two-thread revocation; raising a pair past a departed holder), `RoleRulesTest`, `UsersPostgresIntegrationTest` (a PENDING holder is not the other manager; the kind change closes and reopens the login), `TemporaryPasswordDeliveryTest` (suppressed and queued are not delivered; one reached is), `NotificationsPostgresIntegrationTest` (the outcomes), `JdbcUserScopesPostgresIntegrationTest` (a deactivated grantee and a grant not yet open resolve nothing; an entry ends with its grant), `JwtClaimsMapperTest` (a claim narrows, never widens), `ExternalGrantsIntegrationTest`, `ReadPermissionsInTheControllersTest`, `KeycloakAdminClientIntegrationTest` (enable), `M1SeedLoaderTest`, and the unit suite.

## Next

As of 21 September 2026. The Checkpoint 1 decisions are taken (the skeleton was kept and completed; the project is coop-erp, ADR-051; the toolchain is on the build machine, ADR-052).

- **Sprint 0 (17A section 14), what is left.** Done and reviewed against 17A: S0-01 to S0-07 and S0-10 to S0-13. **S0-07 web shell is done**, in five steps (login; API client; scope banner, module definitions, navigation; design tokens, the three Noto fonts, MoneyDisplay, StateChip, TrainingBadge and the reference page `/_design`; the Playwright smoke test and the e2e stage). Its done criterion is a command: `make up`, then `make e2e`. What is left of Sprint 0 is S0-08 and S0-09, both with the till's owner. **S0-08 till shell:** with the till's owner; two points to raise or record there (Gradle wrapper 8.9 where 17A says 8.10+; `till/agent-test` of 17A section 2 does not exist). **S0-09** (till client generation from the slices) follows S0-08 and needs the Android SDK.
- **Open with the architect.** `CR-17A-2` is accepted (21 Sep) and applied: the repository is on Spring Boot 3.5.16 and Spring Modulith 1.4.13, and the scan went from 6 critical and 25 high to 3 critical and 1 high. The two findings that were left (Tomcat, the PostgreSQL driver) are cured by temporary version overrides, which the build itself asks to be deleted once Spring Boot carries them; the backend scan is at 0 critical and 0 high. Re-issue 17A v0.2 (now also for CR-17A-2's version rows), 28A v0.2 and 00_Start_Here v1.2 for the accepted change requests. The Dependabot pull requests parked on 20 September (the backend patch group with Spring Boot 3.3.13, Keycloak 26, nginx, two web tool updates) are to be reviewed; the Keycloak 26 one can now be tested, because `make e2e` signs in for real and would show what a version change breaks. Branch protection and CODEOWNERS in one to two weeks, which needs the GitHub Team plan. The five `NOTIFY_*` secrets switch on the failure mail. QA review of every Sinhala and Tamil text.
- **Then: the 19A phase, tickets K-01 to K-13**, hello module green throughout, doc 32 section 11 conformance suite passing against the till simulator. The branch `chore/19a-kernel-prep` has prepared it: the version catalogue names every library the tickets need, `kernel.api` carries the three declarations 19A writes out, the kernel migration folder reserves a number range per lane, and `KernelRecorder` records through the `AuditFacade` and `EventPublisher` interfaces so that K-04 and K-05 need no test change. **Decisions of the architect, 21 September**, written out in `docs/PLAN_TO_M2.md` Phase 2: start order K-01, K-04, K-03a; K-03 splits into K-03a (idempotency table and command interceptor order, buildable now, and it closes the hole that the idempotency store is per instance) and K-03b (permission resolver, SoD helpers, snapshot signer); K-13 splits into K-13a (clock, offset, business date service) and K-13b (the day-close part, carried to M6); K-02 arrives in two pull requests, the first accepting the token **or** the development headers with the Playwright suite as the net, the second deleting the headers; the permission resolver is seed-backed until M1-02 brings the catalogue, because the server checks no permission at all today and a hidden button in the web client is only a courtesy; the outbox relay is a database-polling relay behind `BrokerAdapter` first and RabbitMQ second, against the same tests; the A4 PDF renderer moves out of K-06 to the first module that prints (M4); K-08 starts alongside the till's S0-08 with doc 32 section 11 as the joint gate; the estimate is 45 to 55 developer-days against the guide's 27, as an estimate and not a measurement. **One unverified risk is the first task of K-04:** whether PostgreSQL 16 applies `ENABLE`/`FORCE ROW LEVEL SECURITY` and the parent's policies to partitions created later. Audit, outbox and the document tables are partitioned by month, so if it does not, the schema rules test fails at the first month rollover and the new partition is readable across entities until then. It has not been checked.
- **For the architect, from the preparation branch.** The Flyway question is decided (architect, 21 Sep) and applied on the same branch: under the strict setting a lower-numbered migration merged after a higher one makes `migrate()` throw and, since it runs in a `@PostConstruct`, stops the application from starting, on developer machines only, because fresh databases never show it. So `coop-erp.migration.out-of-order` is now a configuration key, **false by default** (a deployed database must be reproducible from the files in order) and **true in the compose stack** (`MIGRATION_OUT_OF_ORDER`), where the lanes' ranges arrive in any order; `MigrationOrderDefaultTest` pins both halves, and doc 35 is to record that the question closes with the first deployed environment. Still open: the larger part of 19A's service map names types without signatures — `IdentityProviderClient`, `DeviceCredentials`, `TextNormaliser`, `Formats`, `DocumentService`, `NumberingService`, `DocumentLinks`, `Attachments`, `SyncApi`, `NotificationDispatcher`, `SnapshotContributor`, `SnapshotChangeLog`, `@ScheduledJob`, and `Resolved` and `Limits` inside `PermissionResolver` — so none of them was declared. Each one blocks the lane that needs it.
- No module (M1 onward) starts before the architect says so.

## Deviations

- **Extracts** are produced with `pdftotext -layout -enc UTF-8 -eol unix`, not bare `-layout`: on Windows the bare form wrote Latin-1 with every dash as an invalid UTF-8 byte.
- **Existing skeleton versus 17A**, to resolve in Step 2: Gradle 9.7.1 wrapper where 17A says 8.10+; no `gradle/libs.versions.toml`; the project is named coop-erp by decision of 18 Sep (17A says knoweb); Spring Boot 3.3.5 and Modulith 1.2.5 pinned inline; no JPA, Security, Testcontainers, OpenAPI generator or Jib; Flyway runs from a `@PostConstruct` in `FlywayConfig`; `ArchitectureTests` lacks 17A's layer rule and own-schema repository rule and has an extra kernel-internals rule; the three `i18n` files are empty; no Makefile, hello module, shared engine, web or till scaffold, compose, CI workflow or `make new-module`.
- **AGENTS.md** lists nine module packages; 17A §2 and the repository also carry `m10procurement` as a reserved package.
- **Build machine** used for this session has no JDK, Docker, Node or make on PATH, so the Checkpoint 2 proof cannot run on it as it stands.
- **Document set:** the register lists two supporting documents (Document_Numbering_RFC, Numbering_Scheme_Tax_Advisor_Briefing) that are in neither the Drive baseline folder nor the repository. Docs 34 to 40 are unwritten; ADR-37 to ADR-50 are not yet in doc 09; glossary 12 lacks the design terms; the Markdown-to-PDF render script has not been chosen.
- **S0-10 versus 17A §10:** the backend image is built by Jib as 17A says (`make image`, plugin 3.5.4 declared in the root build so all plugins share one Jackson version). One addition: `make image` pulls the base image with Docker and passes `-PjibFromDaemon=true`, so Jib reads the base from the local daemon; on a machine not signed in to Docker Hub, Docker Desktop hands Jib an empty credential and the registry answers 401. CI pushes with `./gradlew :app:jib` and leaves the flag off. Keycloak keeps no data volume, so `realm-dev.json` is re-imported whenever its container is recreated and console changes are not kept. The token claims `ent`, `cls`, `lang` and `roles` are provisional until K-02 defines the mapping of doc 19 §1 (`dev`, `scopes`, `rv`, `mfa_at` are absent); the two development entity ids (`0190f000-…-0001` Federation, `…-0002` an MPCS) are shared by the realm and the hello seed. `make test-int` fails on purpose until S0-12 brings integration tests. `.gitattributes` now forces LF for Makefile, Dockerfile, gradlew, YAML, conf and SQL files. Existing local databases created by the earlier compose file had no volume, so nothing needs migrating; after pulling this change run `make reset` once.
- **Build machine:** on this Windows machine every JVM failed to open a selector (`Unable to establish loopback connection`): Java creates a local socket file in the temp folder on C:, and that fails there. Cause not found; workaround is the user environment variable `JAVA_TOOL_OPTIONS=-Djdk.net.unixdomain.tmpdir=D:/tmp`, with which `./gradlew test` (32 tests) and `make up` pass. It is a machine setting, not a repository change.
- **Build machine, 20 Sep: one cause for both faults, probably.** Docker Desktop stopped starting: `listening on unix://C:/Users/.../Docker/run/sailor-ingest.sock: ... The file cannot be accessed by the system` (Windows error 1920), and the same for `docker-secrets-engine/engine.sock`. Windows refuses every unix-socket file on the C: drive, including ones created seconds before; `fsutil reparsepoint query` on them fails the same way, while the `afunix` driver reports RUNNING and sockets on D: work. That is the fault behind the Java selector error too, which is why `-Djdk.net.unixdomain.tmpdir=D:/tmp` cures Java (now set for the Windows user). Docker's socket paths cannot be moved, so the cure to try is a Windows restart; if it returns, look for a file-system filter on C: (antivirus, backup or sync agent) with `fltmc` from an administrator prompt. Two folders were moved aside while trying, and can be deleted after a restart: `%LOCALAPPDATA%Dockerun.stale-*` and `%LOCALAPPDATA%docker-secrets-engine.stale-*`. Never choose "Reset to factory defaults" in Docker's error dialog: it deletes the images and volumes.
- **S0-12 versus 17A §12:** listed with reasons in `hello/README.md`. In short: no `entityId` in the command (taken from the scope); the audit and event stubs still log, because `kernel.audit_event` and `kernel.event_outbox` belong to 19A K-04 and K-05, so the tests count calls, not rows; no `ext_view` policy until K-01; Testcontainers 1.21.4 overrides Boot's 1.19, which Docker Engine 29 refuses. Open proof rows: the till hello screen and till-side `Money.roundCash` parity (not built here, no Android SDK on the build machine). The two-instance Playwright run is closed by S0-07 step 5. The in-memory idempotency store holds for one instance only, until K-03.
- **S0-13 versus 17A:** `make new-module` takes a third argument, `ENTITY` (17A §13 shows `NAME` and `SCHEMA`): without it every table, class and URL of the copy would be named after the schema (`catalogue.catalogue`), and two modules could not coexist because Spring names beans after classes. The copy's permission strings are placeholders (`todo.<schema>.<entity>.register`), because the guides use short codes that cannot be derived (`cat.sku.create`); the build refuses them until they are replaced. `m10procurement` cannot be scaffolded: it has no schema in doc 18 Part F, `FlywayConfig` or the architecture tests. The till screen is not scaffolded, because hello has none yet.
- **S0-11 versus 17A §11, still open and said so at the top of `ci.yml`:** the Android emulator smoke (needs the till hello screen, S0-08; the Playwright run was added to `stack-smoke` with S0-07 step 5); publish (no registry chosen; doc 35 unwritten); coverage floors (set per module by its guide; none for hello); a Java formatting check (no formatter in the build). The pipeline itself could not be run from the build machine: it was validated with actionlint and command by command, and its first real run is on the pull request. The "done when" of 17A, green on `main` with the nightly fresh-clone test, is therefore confirmed only after that run and one night. Branch protection (required checks) is a repository setting and is not in this change.
- **S0-06 decisions (architect, 2026-09-20):** (1) `kernel.api.CurrentScope` is accepted as a kernel contract addition; the module diagram `docs/modules/module-kernel.adoc` shows it. It is for controllers only: the architecture rule `onlyControllersAskForTheCurrentScope` fails the build for a class outside a module's `web` package that uses it, because a handler that reads the scope of the HTTP request cannot serve a till sync batch or a job. (2) Bean validation is on (this reverses the first S0-06 choice): `useBeanValidation=true`, `spring-boot-starter-validation`. The shape of a request is written in the slice and enforced by the kernel before the controller runs; `RequestValidationHandler` answers 400 `request.invalid` with an `errors` list of `{ field, code, message, params }` whose codes are generic, translated message ids (`request.field.required`, `too_short`, `too_long`, `too_small`, `too_large`, `format`, `invalid`), and 400 `request.malformed` for a body that cannot be read. Reason: with trainees building modules in parallel, a forgotten null check must not become a 500. Business rules, and any rule that must hold for commands that do not arrive over HTTP, stay handler guards. `Problem` in `common.yaml` gained `errors` and the `FieldProblem` schema: a deviation from 17A section 4.4, which shows no field list; raised as `docs/change-requests/CR-17A-1.md`, which also asks for one name (`code`, not `type` as 21A section 5 words it) for the message id. Accepted on 2026-09-20; it stays listed here until 17A is re-issued. Hibernate's own entity validation is switched off (`jakarta.persistence.validation.mode: none`). The Sinhala and Tamil texts of the nine new messages are unreviewed, like the others (QA). (3) For a response defined in `common.yaml` the generator names the class after the operation (`RegisterGreeting400Response`); accepted in exchange for one shared definition instead of nine copies. Bundling the slices first would tidy the names and can be added later without touching a module. (4) 17A names `orval or openapi-typescript` for the web client; openapi-typescript stays (types only). S0-07 decides whether the shell's `api/client.ts` uses `openapi-fetch` inside.
- **Close-out sweep decisions (architect, 2026-09-20), to be built with S0-03 and S0-01:** `kernel.api.BusinessClock` is replaced by a `java.time.Clock` bean in UTC, as 19A section 13 says; `BusinessDate.current` takes the location id, as 19A says, because a business date belongs to a location and an entity-wide scope has none; the Gradle 9.7.1 wrapper stays (17A says 8.10+) and is recorded as an ADR with S0-01. Known and not part of S0-05: rule R4 is enforced in doc 17 by one database role per schema, and there is one `app_rw` role for every schema, so native SQL that crosses a schema is caught by review only (S0-04 territory).
- **Change requests accepted (architect, 2026-09-20):** `CR-17A-1` (field list on the problem document; `code`, not `type`, carries the message id), `CR-28A-1` (cost of goods sold comes from M5's `stock.moved.v1`, not from the receipt bundle) and `CR-00_Start_Here-1` (the two registers are `.docx` in the requirements folder). The code already matches CR-17A-1. Open until done: re-issue 17A v0.2, 28A v0.2 and 00_Start_Here v1.2, with the one-sentence amendments to 21A, 25A, 26A and doc 30, and bump the register. Also closed the same day: `JAVA_TOOL_OPTIONS` is set for the Windows user of the build machine; the untracked `docs/reviews/` notes were deleted, their findings being fixed and recorded here.
- **The step-up window, versus doc 19 DR-7 and 19A section 2 (26 Sep, branch `fix/identity-review-2`):** the register key is `security.mfa.freshness` (a duration, twelve hours by default, at most twelve hours, configurable down), not `security.mfa_freshness_hours`; and there is one window for every permission that requires the second factor, where 19A names a per-action list of shorter windows. The list is not built yet: `StepUp` takes no permission. To build with the first action that needs a window shorter than the entity's.
- **Password grant on the dev realm's web client (26 Sep, branch `fix/identity-review-2`):** `coop-erp-web` in `infra/compose/realm-dev.json` keeps `directAccessGrantsEnabled` true, because `tools/smoke.mjs` and `UsersAgainstTheProviderIntegrationTest` sign in with the password grant. On compose a password sign-in then also passes a step-up (`COOP_ERP_MFA_PASSWORD_REAUTH_COUNTS`). Development only; a production realm keeps direct grants off on this client (code and PKCE only), and the backend logs an ERROR at start when password-reauth-counts is on with an issuer that is not a local or `.test` one.
- **No from-side trigger on `document_link` (26 Sep, branch `fix/documents-numbering-review`):** the deep review asked for a BEFORE INSERT trigger on `document_link` that refuses a link whose correcting document is issued. `DocumentLinks` (19A section 7; doc 18, table document_link) links a correcting document to its original after the correction is issued (a reversal is issued, then linked; only SUPERSEDES is drafts on both sides), and the hash does not cover links, so such a trigger would refuse the documented flow. Not added; the from side stays as V0050 has it (written by the owner of the correcting document only).
- **`own_read` and `own_update` admit a NULL location, versus the template (26 Sep, branch `fix/documents-numbering-review`):** on `kernel.numbering_series` and `kernel.document` a shop-scoped session sees and advances the ENTITY series and sees the entity's location-less documents; the template's location line hides every NULL location. Asked for by the wave-1 finding on V0050:92; listed in `RlsMatrixIntegrationTest` as a departure and in the pull request for the architect.
- **The web shell's permissions are still the temporary role map, versus doc 30 section 3 ("visibility from the resolved permission set") (26 Sep, branch `fix/web-shell-review`):** the kernel resolves the set (`PermissionResolver`, K-03b) but no operation of any slice hands it to a client, and the token carries `roles` only. The shell now mirrors the one rule it can without the set (a class other than OWN runs no command) and keeps the map for the rest. To replace with a read of the caller's resolved permissions once the architect names where it lives (a session operation of the kernel slice, or a claim of the token).
- **AmendRole on a template refuses two changes 21A section 6.1 does not name, and two kernel api signatures grew (26 Sep, branch `fix/m1-roles-users-grants-review`):** 21A's pseudocode runs the holder guards on the entity's own roles only; a template is assigned directly in every entity and the Federation cannot read those entities' users, so dropping `gov.user.manage` or adding a FEDERATION-scope code is refused while anybody outside the Federation holds the template (`m1.role.template_held_outside_federation`); every other amendment of a template is as 21A has it. `IdentityProviderClient` gains `enableUser` beyond CR-19A-1's five signatures (the way back for a user whose kind lost the back office), and `Notifications.send` answers a `Delivery` rather than a bare id (19A section 10 says only "returns the notification id"), because a caller handing over a one-time password must know whether anybody was reached. Both to be folded into the next issue of 19A.

- **K-03a Command idempotency** — idempotency now runs inside the
  `@CommandHandler` transaction: the database key is claimed before the handler,
  the command result is recorded last, and concurrent requests on different
  backend instances serialize on PostgreSQL. `kernel.idempotency_key` is
  day-partitioned; retention is partition-drop based and the application role
  has no DELETE or TRUNCATE grant.
- **K-05 Event backbone** â€” domain events are written to the partitioned
  `kernel.event_outbox` in the caller transaction, relayed in per-source order
  through a separate `coop_relay` data source, and published as persistent
  RabbitMQ messages behind `BrokerAdapter`. `@EventConsumer` handlers use
  `kernel.event_inbox` for exactly-once application; three failures go to
  `domain.dlq` with an ALERT audit record, and archived events can be replayed
  to one consumer without changing the producer.